# Test generated by RoostGPT for test petstore-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=f622e49ab3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pets_get.csv"
    fields:
      - "tags"
      - "limit"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/pets"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: "{{ tags }}"
            limit: "{{ limit }}"
          expect:
            - statusCode: 200
            - hasProperty: "tags"
            - hasProperty: "limit"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid_url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: "{{ tags }}"
            limit: "{{ limit }}"
          expect:
            - statusCode: 400
            - hasProperty: "code"
            - hasProperty: "message"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/pets"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: "!!!@@@###$$$%%%^^^&&&***((()))"
            limit: 99999999999999999999
          expect:
            - statusCode: 200
            - hasProperty: "tags"
            - hasProperty: "limit"

  - name: "API request without authentication"
    flow:
      - get:
          url: "/pets"
          qs:
            tags: "{{ tags }}"
            limit: "{{ limit }}"
          expect:
            - statusCode: 401

  - name: "API request with incorrect HTTP method"
    flow:
      - post:
          url: "/pets"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            tags: "{{ tags }}"
            limit: "{{ limit }}"
          expect:
            - statusCode: 405
