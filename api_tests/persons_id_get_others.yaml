# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=131d25110f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "./persons_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Scenario with all failure edge cases"
    flow:
      - log: "Test case 1: Invalid URL"
        get:
          url: "/recipients/persons/invalid_id"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
      - log: "Test case 2: Invalid URL with query parameters"
        get:
          url: "/recipients/persons/{{ id }}?param=value"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
      - log: "Test case 3: Unauthorised request"
        get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
      - log: "Test case 4: Missing required header"
        get:
          url: "/recipients/persons/{{ id }}"
          headers:
            MissingHeader: "value"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
      - log: "Test case 5: Invalid method type"
        post:
          url: "/recipients/persons/{{ id }}"
          expect:
            - statusCode: 405
            - hasProperty: "error"
            - hasProperty: "description"
