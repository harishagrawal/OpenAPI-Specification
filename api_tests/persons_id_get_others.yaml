# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=131d25110f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "./persons_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Scenario with all failure edge cases"
    flow:
      - log: "Test case 1: When the ID is invalid, the API should return a 400 error"
      - think: 1
      - get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "Bearer INVALID_TOKEN"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      - log: "Test case 2: When the authorization token is invalid, the API should return a 401 error"
      - think: 1
      - get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "Bearer INVALID_TOKEN"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - log: "Test case 3: When the URL is invalid, the API should return a 404 error"
      - think: 1
      - get:
          url: "/invalid/url/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
