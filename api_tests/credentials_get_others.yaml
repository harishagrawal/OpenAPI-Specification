# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=66f52a572f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/credentials_get_others.csv"
    fields:
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Credentials API different failure cases"
    flow:
      - comment: "Test with a size value too large"
        get:
          url: "/credentials?size=51"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        log: "Size value exceeds the maximum limit of 50"
        expect:
          - statusCode: 400
          - hasProperty: 'error'
          - hasProperty: 'description'
      - think: 5

      - comment: "Test with a negative size value"
        get:
          url: "/credentials?size=-1"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        log: "Size value is below the minimum limit of 1"
        expect:
          - statusCode: 400
          - hasProperty: 'error'
          - hasProperty: 'description'
      - think: 5

      - comment: "Test with an invalid offset value"
        get:
          url: "/credentials?offset=-1"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        log: "Offset value is negative, which is not allowed"
        expect:
          - statusCode: 400
          - hasProperty: 'error'
          - hasProperty: 'description'
      - think: 5

      - comment: "Test with missing required Authorization header"
        get:
          url: "/credentials"
        log: "Authorization header not provided"
        expect:
          - statusCode: 401
          - hasProperty: 'error'
          - hasProperty: 'description'
      - think: 5

      - comment: "Test with invalid Authorization token"
        get:
          url: "/credentials"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
        log: "Provided an invalid Authorization token"
        expect:
          - statusCode: 401
          - hasProperty: "error"
          - hasProperty: "description"
      - think: 5

      - comment: "Test with an invalid URL"
        get:
          url: "/credentialz"  # Deliberate typo to make URL invalid
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        log: "Invalid URL endpoint, expected '/credentials'"
        expect:
          - statusCode: 404

