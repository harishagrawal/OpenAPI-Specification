# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c5011f3c64

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "size"
      - "offset"
  plugins:
    expect: {}
scenarios:
  - name: "Credentials API test"
    flow:
      - think: 2
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
          log: "Testing invalid size and offset values"
      - think: 2
      - get:
          url: "/credentials?size=51&offset=0"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
          log: "Testing size value greater than maximum limit"
      - think: 2
      - get:
          url: "/credentials?size=20&offset=-1"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
          log: "Testing negative offset value"
      - think: 2
      - get:
          url: "/invalid-url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          log: "Testing invalid URL"
      - think: 2
      - get:
          url: "/credentials?size=20&offset=0"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
          log: "Testing invalid authorization token"
