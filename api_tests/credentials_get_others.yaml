# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=66f52a572f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/credentials_get_others.csv"
    skipHeader: true
    fields:
      - "size"
      - "offset"
  plugins:
    expect: {}

scenarios:
  - name: "Negative tests for credentials API"
    flow:
      - post:
          url: "/credentials"
          log: "Testing credential creation with no authentication."
          headers:
            Authorization: "InvalidAuth {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401

      - think: 2

      - get:
          url: "/credentials/invalid-endpoint"
          log: "Testing GET request with invalid URL."
          expect:
            - statusCode: 404

      - think: 2

      - get:
          url: "/credentials"
          log: "Testing GET request with missing required token."
          headers: {}
          expect:
            - statusCode: 401

      - think: 2

      - get:
          url: "/credentials?size=-1"  # Invalid query parameter, negative size not allowed, should be minimum 1
          log: "Testing GET request with size less than the allowed minimum."
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"

      - think: 2

      - get:
          url: "/credentials?size=100"  # Invalid query parameter, size greater than allowed maximum, should be maximum 50
          log: "Testing GET request with size more than the allowed maximum."
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"

      - think: 2

      - get:
          url: "/credentials?offset=-1"  # Invalid query parameter, negative offset not allowed, should be minimum 0
          log: "Testing GET request with offset less than the allowed minimum."
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"

