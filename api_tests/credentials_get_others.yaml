# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c5011f3c64

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Test /credentials endpoint"
    flow:
      - log: "Test Scenario: Unauthorized access"
        get:
          url: "/credentials"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasProperty: "error"
        think: 1
      - log: "Test Scenario: Invalid url"
        get:
          url: "/invalid_url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Test Scenario: Invalid query parameters"
        get:
          url: "/credentials?size=100&offset=-1"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
            - hasProperty: "schema_field"
            - hasProperty: "value"
        think: 1
      - log: "Test Scenario: Missing query parameters"
        get:
          url: "/credentials"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        think: 1
