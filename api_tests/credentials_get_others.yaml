# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=66f52a572f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/credentials_get_others.csv"
    fields:
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Credential API Error Handling Scenario"
    flow:
      - comment: "Test with offset lower than allowed minimum of 0 - expected to return 400 Bad Request"
        get:
          url: "/credentials?offset=-1"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        log: "Request with invalid 'offset' value should return 400 Bad Request"
      - think: 1

      - comment: "Test with size greater than allowed maximum of 50 - expected to return 400 Bad Request"
        get:
          url: "/credentials?size=51"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        log: "Request with invalid 'size' value should return 400 Bad Request"
      - think: 1

      - comment: "Test with missing required security field in the header - expected to return 401 Unauthorized"
        get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "invalidtoken"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasProperty: "error"
        log: "Request without valid authentication should return 401 Unauthorized"
      - think: 1

      - comment: "Test with invalid URL to induce failure - expected to return 400 Bad Request"
        get:
          url: "/credntials?size={{ size }}" # intentionally misspelled "credentials"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        log: "Request with invalid URL should return 400 Bad Request"

