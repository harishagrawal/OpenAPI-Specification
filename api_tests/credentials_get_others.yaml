# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c5011f3c64

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_get_others.csv"
    fields:
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Test /credentials endpoint"
    flow:
      - log: "Test Scenario: Unauthorized access"
      - get:
          url: "/credentials"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
      - think: 5
      - log: "Test Scenario: Invalid URL"
      - get:
          url: "/invalid_url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 5
      - log: "Test Scenario: Invalid query parameter"
      - get:
          url: "/credentials?size=invalidsize"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
      - think: 5
      - log: "Test Scenario: Exceeding maximum limit of size"
      - get:
          url: "/credentials?size=51"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
      - think: 5
      - log: "Test Scenario: Negative offset"
      - get:
          url: "/credentials?offset=-1"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
