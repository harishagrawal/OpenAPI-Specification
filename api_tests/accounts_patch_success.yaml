# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /accounts_patch for http method type PATCH in artillery framework
# RoostTestHash=2758affb7e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "accounts_patch_success.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Login to existing Account"
    flow:
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - hasProperty: "headers.api-version"
          log: "Login to existing account using valid email"
      - think: 1
