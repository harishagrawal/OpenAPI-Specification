# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=2758affb7e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  processor: "./processors.js"
  payload:
    path: "/mnt/data/accounts_patch_success.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Valid Login Request Scenario"
    flow:
      - post:
          url: "/accounts"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - hasHeader: 'api-version'
          log: "Performing a valid login with provided email"
          think: 1
# Expected behavior: Should login successfully with valid email and receive API version in response headers.

