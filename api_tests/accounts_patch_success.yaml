# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /accounts_patch for http method type PATCH in artillery framework
# RoostTestHash=2758affb7e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/accounts_patch_success.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Login to existing account with valid email"
    flow:
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            content-type: "application/json"
          # Tests a successful login attempt with a valid email address
          log: "Sending login request with a valid email: {{ email }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
          think: 1

