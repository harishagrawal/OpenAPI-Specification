# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=2758affb7e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/accounts_patch_success.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Login to existing Account"
    flow:
      - log: "Attempt to login with valid email"
      - post:
          url: "/accounts"
          json: 
            email: "{{ email }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: 'api-version'
      - think: 1

      - log: "Attempt to login with invalid email pattern"
      - post:
          url: "/accounts"
          json:
            email: "invalid-email"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: 'api-version'
            - responseBody: 
                hasProperties: ['description', 'error']
      - think: 1

