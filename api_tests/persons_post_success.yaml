# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/persons_post for http method type POST in artillery framework
# RoostTestHash=b4614c0581

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "persons_post_success.csv"
    fields:
      - "initials"
      - "firstName"
      - "middleName"
      - "lastName"
      - "birthDate"
      - "birthPlace"
      - "phone"
      - "email"
      - "postalCode"
      - "houseNumber"
      - "houseNumberPostfix"
      - "countryCode"
      - "reference"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test /recipients/persons API"
    flow:
      - log: "Create new person-recipient of a Credential"
      - post:
          url: "/recipients/persons"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            initials: "{{ initials }}"
            firstName: "{{ firstName }}"
            middleName: "{{ middleName }}"
            lastName: "{{ lastName }}"
            birthDate: "{{ birthDate }}"
            birthPlace: "{{ birthPlace }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
            postalCode: "{{ postalCode }}"
            houseNumber: "{{ houseNumber }}"
            houseNumberPostfix: "{{ houseNumberPostfix }}"
            countryCode: "{{ countryCode }}"
            reference: "{{ reference }}"
          expect:
            - statusCode: 201
            - hasProperty: "headers['api-version']"
            - hasProperty: "headers.location"
            - hasProperty: "body.id"
            - hasProperty: "body.initials"
            - hasProperty: "body.firstName"
            - hasProperty: "body.middleName"
            - hasProperty: "body.lastName"
            - hasProperty: "body.birthDate"
            - hasProperty: "body.birthPlace"
            - hasProperty: "body.phone"
            - hasProperty: "body.email"
            - hasProperty: "body.postalCode"
            - hasProperty: "body.houseNumber"
            - hasProperty: "body.houseNumberPostfix"
            - hasProperty: "body.countryCode"
            - hasProperty: "body.reference"
            - hasProperty: "body.createdAt"
            - hasProperty: "body.updatedAt"
      - think: 1
