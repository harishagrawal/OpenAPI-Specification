# Test generated by RoostGPT for test mixtral-8x-api using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat


# Test generated for /products/{productId}_delete for http method type DELETE in artillery framework

# RoostTestHash=befb82c928


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 30
      arrivalRate: 10
  plugins:
    expect: {}

scenarios:
  - flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          thinkTime: 1s
          expect:
            - statusCode: 200
            - contentType: application/json
            - hasProperty: productId
            - hasProperty: name
            - hasProperty: description
            - hasProperty: price
      - delete:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          thinkTime: 1s
          expect:
            - statusCode: 200
            - contentType: application/json
            - equals: {"message": "Product deleted"}

payload:
  productId: "products_productId_delete_success.csv"
  skipHeader: true
