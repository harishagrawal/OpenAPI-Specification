# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /recipients/branches_get for http method type GET in artillery framework
# RoostTestHash=74a40da00e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "branches_get_success.csv"
    fields:
      - "cocNumber"
      - "cocBranchNumber"
      - "tradeName"
      - "size"
      - "offset"
    skipHeader: true

scenarios:
  - name: "Test Recipient Branch API"
    flow:
      - log: "Sending GET request with valid payload"
      - get:
          url: "/recipients/branches?coc-number={{ cocNumber }}&coc-branch-number={{ cocBranchNumber }}&trade-name={{ tradeName }}&size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "branches"
            - hasProperty: "total"
            - header: "api-version"
            - header: "page-size"
            - header: "page-offset"
      - think: 1
