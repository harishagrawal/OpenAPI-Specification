# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "API robustness test"
    flow:
      - log: "Test case-1: API should return 400 if the request payload is invalid"
        post:
          url: "/me"
          json:
            name: "invalid name"
            image: "invalid image"
            settings: "invalid settings"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1

      - log: "Test case-2: API should return 401 if the authorization token is invalid"
        post:
          url: "/me"
          json:
            name: "valid name"
            image: "valid image"
            settings: "valid settings"
          headers:
            Authorization: "invalid-token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1

      - log: "Test case-3: API should return 400 if the URL is invalid"
        post:
          url: "/invalid-url"
          json:
            name: "valid name"
            image: "valid image"
            settings: "valid settings"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 1
