# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./processor.js"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "./me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true

scenarios:
  - name: "Test /me endpoint for failure cases"
    flow:
      - log: "Invalid URL test"
      - post:
          url: "/wrong/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
      
      - log: "Test for unauthorized access"
      - post:
          url: "/me"
          headers:
            Authorization: "invalid_token"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1

      - log: "Test for bad request"
      - post:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "invalid_image"
            settings: "{{ settings }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
