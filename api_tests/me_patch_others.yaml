# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Invalid input data and authentication tests for /me endpoint"
    flow:
      - log: "Sending an invalid name to test validation error"
        post:
          url: "/me"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "!nv@lidName2023"
            image: "{{ image }}"
            settings: "{{ settings }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"

      - think: 2

      - log: "Sending an empty payload to test Bad Request"
        post:
          url: "/me"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json: {}
        expect:
          - statusCode: 400
          - hasProperty: "error"

      - think: 2

      - log: "Using invalid Authorization header to test authentication failure"
        post:
          url: "/me"
          headers: 
            Authorization: "Bearer wrong_token"
            Content-Type: "application/json"
          json:
            name: "Jane Doe"
            image: "{{ image }}"
            settings: "{{ settings }}"
        expect:
          - statusCode: 401
          - hasProperty: "error"

      - think: 2

      - log: "Sending a correct payload with a malformed image Data URL to test validation error on the image"
        post:
          url: "/me"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "Jane Doe"
            image: "malformedimagedata"
            settings: "{{ settings }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"

      - think: 2

      - log: "Sending request to an invalid URL to test routing failure"
        post:
          url: "/invalid_endpoint"
          headers: 
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "Jane Doe"
            image: "{{ image }}"
            settings: "{{ settings }}"
        expect:
          - statusCode: 404

