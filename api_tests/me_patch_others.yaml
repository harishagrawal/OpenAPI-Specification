# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  processor: "./my_processor.js"

scenarios:
  - name: "Test /me endpoint"
    flow:
      - log: "Test Case 1: Test with invalid URL"
        post:
          url: "/invalidurl"
          expect:
            - statusCode: 404
        think: 1

      - log: "Test Case 2: Test with invalid auth token"
        post:
          url: "/me"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
        think: 1

      - log: "Test Case 3: Test with invalid payload"
        post:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: ""
          expect:
            - statusCode: 400
        think: 1

      - log: "Test Case 4: Test with missing payload"
        post:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
          expect:
            - statusCode: 400
        think: 1
