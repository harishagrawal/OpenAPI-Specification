# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true

scenarios:
  - name: "Error handling scenarios for /me PATCH endpoint"
    flow:
      - comment: "Test case with a missing 'name' field in JSON payload which should result in bad request error"
        post:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            image: "{{ image }}"
            settings: "{{ settings }}"
          log: "Sending a PATCH request to /me with missing 'name'. Expecting 400 Bad Request."
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasProperty: 'description'
        think: 2

      - comment: "Test case with invalid data in 'image' field (not base64 encoded) which should result in bad request error"
        post:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "invalid_base64"
            settings: "{{ settings }}"
          log: "Sending a PATCH request to /me with invalid 'image' field. Expecting 400 Bad Request."
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasProperty: 'description'
        think: 2

      - comment: "Test case for authorization failure by sending incorrect auth token"
        post:
          url: "/me"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          log: "Sending a PATCH request to /me with incorrect Authorization token. Expecting 401 Authentication Required."
          expect:
            - statusCode: 401
            - hasProperty: 'error'
            - hasProperty: 'description'
        think: 2

      - comment: "Test case with an invalid URL to trigger a not found error"
        post:
          url: "/me-invalid-url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          log: "Sending a PATCH request to an invalid URL /me-invalid-url. Expecting 404 Not Found."
          expect:
            - statusCode: 404
            - hasProperty: 'error'
            - hasProperty: 'description'
        think: 2

