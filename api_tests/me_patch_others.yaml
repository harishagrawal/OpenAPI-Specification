# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/me_patch_others.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test /me endpoint with failure cases"
    flow:
      - think: 1

      - post:
          url: "/me" # Invalid URL case for validation failure
          log: "Sending request with an invalid URL should result in a Not Found error"
          headers:
            Authorization: "INVALID {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 404
            # Expected behavior: Endpoint should return Not Found error

      - think: 2

      - post:
          url: "/me"
          log: "Sending request with no payload should result in Bad Request error"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400 # Bad request status code
            # Expected behavior: Should return Bad Request due to missing payload

      - think: 2

      - post:
          url: "/me"
          log: "Sending request with invalid 'name' field in payload should result in Bad Request error"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ invalidName }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'

      - think: 2

      - post:
          url: "/me"
          log: "Sending request with invalid 'image' field in payload should result in Bad Request error"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "invalid_image_string"
            settings: "{{ settings }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'

      - think: 2

      - post:
          url: "/me"
          log: "Sending request with invalid Authorization header should result in Authentication Required error"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 401
            - hasProperty: 'error'

