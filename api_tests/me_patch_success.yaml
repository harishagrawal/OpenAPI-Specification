# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/me_patch_success.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Change account settings scenario"
    flow:
      - post:
          url: "/me"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Sending request to change name, image, and settings of the user account"
          # Change user name, image and settings with valid inputs
          think: 1
          expect:
            - statusCode: 200 
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
      - post:
          url: "/me"
          json:
            name: "{{ name }}"
            settings: "{{ settings }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Sending request to change name and settings of the user account with no image"
          # Change user name and settings without providing an image
          think: 1
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "settings"
            - hasHeader: "api-version"

