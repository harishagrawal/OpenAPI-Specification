# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /me_patch for http method type PATCH in artillery framework
# RoostTestHash=6dd436357d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/me_patch_success.csv"
    fields:
      - "name"
      - "image"
      - "settings"
    skipHeader: true
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
  plugins:
    expect: {}

scenarios:
  - name: "Update account settings scenario"
    flow:
      - post:
          url: "/me"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          # Update user account settings with valid input payload
          log: "Updating user settings"
          headers:
            content-type: "application/json"
          expect:
            - statusCode: 200
              headers:
                - "api-version"
      - think: 1

      - post:
          url: "/me"
          json:
            name: "{{ name }}"
            # Simulate update with image field as null and valid settings
            image: null
            settings: "{{ settings }}"
          # Update user name and settings without an image
          log: "Updating user name and settings without image"
          headers:
            content-type: "application/json"
          expect:
            - statusCode: 200
              headers:
                - "api-version"
      - think: 1

      - post:
          url: "/me"
          json:
            # Simulate update with only name field and null image and settings
            name: "{{ name }}"
            image: null
            settings: null
          # Update only user name with null image and settings
          log: "Updating just the user name with null image and settings"
          headers:
            content-type: "application/json"
          expect:
            - statusCode: 200
              headers:
                - "api-version"
      - think: 1

