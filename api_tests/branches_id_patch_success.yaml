# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /recipients/branches/{id}_patch for http method type PATCH in artillery framework
# RoostTestHash=9caaa2ef96

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "branches_id_patch_success.csv"
    fields:
      - "id"
      - "tradeName"
      - "phone"
      - "email"
  plugins:
    expect: {}

scenarios:
  - name: "Testing /recipients/branches/{id} API endpoint"
    flow:
      - log: "Starting test for /recipients/branches/{id} API endpoint"
      - think: 1
      - post:
          url: "/recipients/branches/{{ id }}"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "tradeName"
            - hasProperty: "phone"
            - hasProperty: "email"
            - hasHeader: "api-version"
          capture:
            - json: "$.id"
              as: "id"
            - json: "$.tradeName"
              as: "tradeName"
            - json: "$.phone"
              as: "phone"
            - json: "$.email"
              as: "email"
      - think: 1
      - log: "Test for /recipients/branches/{id} API endpoint completed successfully"
