# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/branches/{id}_patch for http method type PATCH in artillery framework
# RoostTestHash=9caaa2ef96

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "branches_id_patch_success.csv"
    fields:
      - "id"
      - "tradeName"
      - "phone"
      - "email"
  plugins:
    expect: {}

scenarios:
  - name: "Testing recipient branches API"
    flow:
      - log: "Sending PATCH request to /recipients/branches/{id} endpoint"
      - patch:
          url: "/recipients/branches/{{ id }}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "cocBranchNumber"
            - hasProperty: "cocNumber"
            - hasProperty: "createdAt"
            - hasProperty: "deregistrationDate"
            - hasProperty: "eoriNumber"
            - hasProperty: "id"
            - hasProperty: "tradeName"
            - hasProperty: "tradeNames"
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
      - think: 1
