# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /recipients/branches/{id}_patch for http method type PATCH in artillery framework
# RoostTestHash=9caaa2ef96

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "branches_id_patch_success.csv"
    fields:
      - "id"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Recipient Branch API Test"
    flow:
      - log: "Testing PATCH request on /recipients/branches/{id}"
      - post:
          url: "/recipients/branches/{{ id }}"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          capture:
            - json: "$"
              as: "response"
          expect:
            - statusCode: 200
            - bodyContains: 
                - "{{ tradeName }}"
                - "{{ phone }}"
                - "{{ email }}"
            - hasProperty: "tradeName"
            - hasProperty: "phone"
            - hasProperty: "email"
      - think: 1
