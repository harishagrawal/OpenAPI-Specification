# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=c5011f3c64

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "./credentials_get_success.csv"
    fields:
      - "size"
      - "offset"
    skipHeader: true

scenarios:
  - name: "Test /credentials endpoint"
    flow:
      - log: "Sending GET request to /credentials with valid size and offset"
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "credentials"
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
        # This is a test to check if the server correctly handles a GET request to /credentials with valid size and offset
      - think: 1
