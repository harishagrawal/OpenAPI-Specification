# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=66f52a572f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_get_success.csv"
    fields:
      - "size"
      - "offset"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test scenario for GET /credentials endpoint with different query parameters"
    flow:
      - log: "Requesting credentials with default query parameters"
      - get:
          url: "/credentials"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: 'credentials'
            - hasProperty: 'total'
            - hasHeader: 'api-version'
            - hasHeader: 'page-size'
            - hasHeader: 'page-offset'
        think: 1

      - log: "Requesting credentials with a specific 'size' query parameter"
      - get:
          url: "/credentials"
          qs:
            size: "{{ size }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: 'credentials'
            - hasProperty: 'total'
            - hasHeader: 'api-version'
            - hasHeader: 'page-size'
            - hasHeader: 'page-offset'
        think: 1

      - log: "Requesting credentials with both 'size' and 'offset' query parameters"
      - get:
          url: "/credentials"
          qs:
            size: "{{ size }}"
            offset: "{{ offset }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: 'credentials'
            - hasProperty: 'total'
            - hasHeader: 'api-version'
            - hasHeader: 'page-size'
            - hasHeader: 'page-offset'
        think: 1


