# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=66f52a572f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/credentials_get_success.csv"
    fields:
      - "size"
      - "offset"
    skipHeader: true
  environments:
    default:
      API_HOST: "https://api.example.com"
      AUTH_TOKEN: "Bearer your-auth-token"
  plugins:
    expect: {}

scenarios:
  - name: "Credentials API testing"
    flow:
      - comment: "Expect to retrieve a limited number of credentials successfully with the size parameter"
        get:
          url: "/credentials?size={{ size }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Retrieve limited number of credentials"
          capture:
            json: "$.credentials.*"
            as: "retrieved_credentials"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "credentials"
            - header: "api-version"
            - header: "page-size"
            - header: "page-offset"
        think: 1

      - comment: "Expect to retrieve credentials using size and offset parameters"
        get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Retrieve credentials with size and offset"
          capture:
            json: "$.credentials.*"
            as: "retrieved_credentials_with_offset"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "credentials"
            - header: "api-version"
            - header: "page-size"
            - header: "page-offset"
        think: 1

      - comment: "Expect to retrieve credentials without providing size and offset"
        get:
          url: "/credentials"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Retrieve credentials without size and offset"
          capture:
            json: "$.credentials.*"
            as: "all_retrieved_credentials"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "credentials"
            - header: "api-version"
            - header: "page-size"
            - header: "page-offset"
        think: 1

