import csv
import random
import faker

# Initialize Faker Library
fake = faker.Faker()

def generate_dummy_data(num_records=10):
    # Define CSV file columns
    fieldnames = ['id', 'name', 'email', 'birthdate', 'phone_number']

    # Create dummy data
    with open('dummy_data.csv', 'w', newline='') as csvfile:
        writer = csv.DictWriter(csvfile, fieldnames=fieldnames)

        writer.writeheader()
        for _ in range(num_records):
            writer.writerow({
                'id': random.randint(1, 100),
                'name': fake.name(),
                'email': fake.email(),
                'birthdate': fake.date_of_birth(minimum_age=18, maximum_age=90),
                'phone_number': fake.phone_number(),
            })

# Generate dummy data for 100 records
generate_dummy_data(100)
