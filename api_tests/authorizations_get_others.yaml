# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=3b64f22044

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  defaults:
    headers:
      api-version: '1.0.0'

scenarios:
  - name: "Authorization endpoint failure cases"
    flow:
      - log: "Testing with invalid URL"
        get:
          url: "/invalid-url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 5

      - log: "Testing without authorization token"
        get:
          url: "/authorizations"
          expect:
            - statusCode: 401
            - hasProperty: 'error'
            - hasProperty: 'description'
      - think: 5

      - log: "Testing with invalid authorization token"
        get:
          url: "/authorizations"
          headers:
            Authorization: "invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: 'error'
            - hasProperty: 'description'
      - think: 5

      - log: "Testing with wrong api-version"
        get:
          url: "/authorizations"
          headers:
            api-version: '2.0.0'
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasProperty: 'description'
      - think: 5
