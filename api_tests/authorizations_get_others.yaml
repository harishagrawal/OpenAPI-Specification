# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=3b64f22044

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "authorizations_get_others.csv"
    fields: ["name", "email"]
    skipHeader: true
scenarios:
  - name: "Authorization API test"
    flow:
      - log: "Starting API test with invalid URL"
      - get:
          url: "/invalid-url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Testing API without authorization"
      - get:
          url: "/authorizations"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing API with invalid authorization"
      - get:
          url: "/authorizations"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing API with invalid payload"
      - get:
          url: "/authorizations"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "invalid-name"
            email: "invalid-email"
          expect:
            - statusCode: 400
      - think: 1
