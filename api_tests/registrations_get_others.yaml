# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=49b35fa9c1

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "id"
      - "credential-url"
      - "size"
      - "offset"
    skipHeader: true

scenarios:
  - name: "API robustness tests"
    flow:
      - log: "Sending request with invalid URL"
        get:
          url: "/invalid_url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
      - log: "Sending request with invalid authorization token"
        get:
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Authorization: "invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
      - log: "Sending request with invalid query parameters"
        get:
          url: "/recipients/branches/{{ id }}/registrations?size=100&offset=-1"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
      - log: "Sending request with missing required path parameter"
        get:
          url: "/recipients/branches//registrations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
