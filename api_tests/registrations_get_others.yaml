# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=49b35fa9c1

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "id"
      - "credential-url"
      - "size"
      - "offset"
    skipHeader: true

scenarios:
  - name: "API robustness tests"
    flow:
      - log: "Sending request with invalid URL"
        get:
          url: "/invalid_url"
          expect:
            - statusCode: 404
        think: 1

      - log: "Sending request without authorization"
        get:
          url: "/recipients/branches/{{ id }}/registrations"
          expect:
            - statusCode: 401
        think: 1

      - log: "Sending request with invalid authorization"
        get:
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
        think: 1

      - log: "Sending request with invalid query parameters"
        get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url=invalid&size=100&offset=-1"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 1

      - log: "Sending request with invalid path parameter"
        get:
          url: "/recipients/branches/invalid/registrations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
        think: 1
