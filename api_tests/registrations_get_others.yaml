# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=49b35fa9c1

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "registrations_get_others.csv"
    fields:
      - "id"
      - "credential-url"
      - "size"
      - "offset"
    skipHeader: true

scenarios:
  - name: "API robustness tests"
    flow:
      - log: "Sending request with invalid URL"
        get:
          url: "/invalid_url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "description"
        think: 1
      - log: "Sending request without auth token"
        get:
          url: "/recipients/branches/{{ id }}/registrations"
        expect:
          - statusCode: 401
          - hasProperty: "error"
          - hasProperty: "description"
        think: 1
      - log: "Sending request with invalid auth token"
        get:
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Authorization: "InvalidToken"
        expect:
          - statusCode: 401
          - hasProperty: "error"
          - hasProperty: "description"
        think: 1
      - log: "Sending request with invalid query parameters"
        get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url=invalid&size=invalid&offset=invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "description"
          - hasProperty: "schema_field"
          - hasProperty: "value"
        think: 1
