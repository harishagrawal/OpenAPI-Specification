# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=e0cf5b2e0e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "login_post_others.csv"
    fields:
      - "client"
      - "email"
      - "otp"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Login API test"
    flow:
      - log: "Testing with invalid email"
        post:
          url: "/login"
          json:
            client: "{{ client }}"
            email: "invalidemail"
            otp: "{{ otp }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
            - hasProperty: "schema_field"
            - hasProperty: "value"
        think: 1
      - log: "Testing with invalid otp"
        post:
          url: "/login"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "invalidotp"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
            - hasProperty: "schema_field"
            - hasProperty: "value"
        think: 1
      - log: "Testing with invalid URL"
        post:
          url: "/invalidurl"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "{{ otp }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Testing with invalid authorization token"
        post:
          url: "/login"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "{{ otp }}"
          headers:
            Authorization: "Invalid token"
          expect:
            - statusCode: 401
        think: 1
