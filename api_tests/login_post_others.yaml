# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=3122292ee3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "login_post_others.csv"
    fields:
      - "client"
      - "email"
      - "otp"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - flow:
      - comment: "Test with invalid email format"
        post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            client: "{{ client }}"
            email: "invalid_email_format"
            otp: "{{ otp }}"
          auth:
            - "bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
        log: "Sending invalid email format should result in a 400 Bad Request response with respective error description"

      - think: 1

      - comment: "Test with incorrect OTP format (less than 6 digits)"
        post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "123"
          auth:
            - "bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
        log: "Sending incorrect OTP format should result in a 400 Bad Request response with respective error description"

      - think: 1

      - comment: "Test with unsupported client type"
        post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            client: "unsupported_client_type"
            email: "{{ email }}"
            otp: "{{ otp }}"
          auth:
            - "bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
        log: "Sending an unsupported client type should result in a 400 Bad Request response with respective error description"

      - think: 1

      - comment: "Test with missing required field 'client'"
        post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
          auth:
            - "bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
        log: "Omitting a required field 'client' should result in a 400 Bad Request response with respective error description"

      - think: 1

      - comment: "Test with invalid authorization token"
        post:
          url: "/login"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer invalid_auth_token"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "{{ otp }}"
          expect:
            - statusCode: 401
            - hasHeader: "api-version"
        log: "Using an invalid authorization token should result in a 401 Unauthorized response"

      - think: 1

      - comment: "Test request with an invalid URL"
        post:
          url: "/login_invalid_url"
          headers:
            Content-Type: "application/json"
          json:
            client: "{{ client }}"
            email: "{{ email }}"
            otp: "{{ otp }}"
          auth:
            - "bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        log: "Sending request to an invalid URL should result in a 404 Not Found response"

