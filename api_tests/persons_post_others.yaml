# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=b4614c0581

config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    maxErrorRate: 0
  plugins:
    expect: {}
  payload:
    path: "persons_post_others.csv"
    fields:
      - "birthDate"
      - "initials"
      - "lastName"
      - "firstName"
      - "middleName"
      - "birthPlace"
      - "phone"
      - "email"
      - "postalCode"
      - "houseNumber"
      - "houseNumberPostfix"
      - "countryCode"
      - "reference"
    skipHeader: true

scenarios:
  - name: "Test recipients/persons API endpoint"
    flow:
      - think: 1
      - log: "Testing without authorization"
      - post:
          url: "/recipients/persons"
          headers:
            content-type: "application/json"
          json:
            birthDate: "{{ birthDate }}"
            initials: "{{ initials }}"
            lastName: "{{ lastName }}"
            firstName: "{{ firstName }}"
            middleName: "{{ middleName }}"
            birthPlace: "{{ birthPlace }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
            postalCode: "{{ postalCode }}"
            houseNumber: "{{ houseNumber }}"
            houseNumberPostfix: "{{ houseNumberPostfix }}"
            countryCode: "{{ countryCode }}"
            reference: "{{ reference }}"
          expect:
            statusCode: 401
            headers:
              api-version: "1.0.0"

      - think: 1
      - log: "Testing with invalid fields"
      - post:
          url: "/recipients/persons"
          headers:
            content-type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            birthDate: "{{ birthDate }}"
            initials: "invalid_initials"
            lastName: "{{ lastName }}"
            firstName: "{{ firstName }}"
            middleName: "{{ middleName }}"
            birthPlace: "{{ birthPlace }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
            postalCode: "{{ postalCode }}"
            houseNumber: "{{ houseNumber }}"
            houseNumberPostfix: "{{ houseNumberPostfix }}"
            countryCode: "{{ countryCode }}"
            reference: "{{ reference }}"
          expect:
            statusCode: 400
            headers:
              api-version: "1.0.0"

      - think: 1
      - log: "Testing with invalid URL"
      - post:
          url: "/invalid_url"
          headers:
            content-type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            birthDate: "{{ birthDate }}"
            initials: "{{ initials }}"
            lastName: "{{ lastName }}"
            firstName: "{{ firstName }}"
            middleName: "{{ middleName }}"
            birthPlace: "{{ birthPlace }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
            postalCode: "{{ postalCode }}"
            houseNumber: "{{ houseNumber }}"
            houseNumberPostfix: "{{ houseNumberPostfix }}"
            countryCode: "{{ countryCode }}"
            reference: "{{ reference }}"
          expect:
            statusCode: 404
