# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=1ce4bf6e3c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "branches_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
scenarios:
  - name: "Test scenario for /recipients/branches/{id} API"
    flow:
      - log: "Sending a request with invalid id"
        get:
          url: "/recipients/branches/invalid-id"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 5
      - log: "Sending a request without authorization header"
        get:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
        think: 5
      - log: "Sending a request with invalid URL"
        get:
          url: "/invalid-url/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 5
