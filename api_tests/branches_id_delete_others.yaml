# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=1ce4bf6e3c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "branches_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Test scenario for /recipients/branches/{id} API"
    flow:
      - log: "Test Case: Invalid URL request"
        get:
          url: "/recipients/branches/invalid-url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "headers.api-version"
            - hasProperty: "content.application.json"
        think: 1
      - log: "Test Case: Unauthorized request"
        get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "headers.api-version"
            - hasProperty: "content.application.json"
        think: 1
      - log: "Test Case: Invalid UUID request"
        get:
          url: "/recipients/branches/invalid-uuid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "headers.api-version"
            - hasProperty: "content.application.json.schema.anyOf"
        think: 1
