# Test generated by RoostGPT for test mixtral-8x-api using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat


# Test generated for /products/{productId}_delete for http method type DELETE in artillery framework

# RoostTestHash=befb82c928


config:
  target: "http://{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 10
  plugins:
    http:
      enabled: true
      pool: true
    expect:
      enabled: true
  variables:
    - key: productId
      value: 1
    - key: authToken
      value: "{{ $processEnvironment.AUTH_TOKEN }}"
    - key: productPayload
      value: "@products_productId_delete_others.csv"
  defaults:
    headers:
      Content-Type: application/json
      Authorization: "Bearer {{authToken}}"

scenarios:
  - flow:
      - get:
          url: "/products/{{productId}}"
          headers:
            Authorization: "Bearer {{authToken}}"
          thinkTime: 1s
          expect:
            - statusCode: 200
            - contentType: application/json
            - hasProperty: productId
            - hasProperty: name
            - hasProperty: description
      - post:
          url: "/products/{{productId}}"
          headers:
            Authorization: "Bearer {{authToken}}"
          json:
            name: "Test Product"
            description: "Test Description"
          thinkTime: 1s
          expect:
            - statusCode: 405
            - contentType: application/json
            - hasProperty: message
      - patch:
          url: "/products/{{productId}}"
          headers:
            Authorization: "Bearer {{authToken}}"
          json:
            name: "Updated Test Product"
          thinkTime: 1s
          expect:
            - statusCode: 405
            - contentType: application/json
            - hasProperty: message
      - delete:
          url: "/products/{{productId}}"
          headers:
            Authorization: "Bearer {{authToken}}"
          thinkTime: 1s
          expect:
            - statusCode: 200
            - contentType: application/json
            - hasProperty: message
      - get:
          url: "/products/invalidUrl"
          headers:
            Authorization: "Bearer {{authToken}}"
          thinkTime: 1s
          expect:
            - statusCode: 404
            - contentType: application/json
            - hasProperty: message
