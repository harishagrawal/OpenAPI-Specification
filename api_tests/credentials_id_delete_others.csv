import csv
import random
import string
import faker

# Create a Faker instance
fake = faker.Faker()

# Define the headers
headers = ['id', 'name', 'email', 'phone', 'dob', 'address']

# Open a CSV file in write mode
with open('dummy_data.csv', 'w', newline='') as csvfile:
    # Create a CSV writer
    writer = csv.writer(csvfile)

    # Write the headers
    writer.writerow(headers)

    # Generate 100 rows of dummy data
    for _ in range(100):
        # Generate dummy values
        id = ''.join(random.choices(string.ascii_letters + string.digits, k=5)) if random.choice([True, False]) else random.randint(1, 10000)
        name = fake.name() if random.choice([True, False]) else random.randint(1, 10000)
        email = fake.email() if random.choice([True, False]) else random.randint(1, 10000)
        phone = fake.phone_number().replace(",", "") if random.choice([True, False]) else ''.join(random.choices(string.ascii_letters + string.digits, k=10))
        dob = fake.date_of_birth(minimum_age=18, maximum_age=90) if random.choice([True, False]) else ''.join(random.choices(string.ascii_letters + string.digits, k=10))
        address = fake.address().replace(",", "").replace("\n", " ") if random.choice([True, False]) else random.randint(1, 10000)

        # Write the row
        writer.writerow([id, name, email, phone, dob, address])
