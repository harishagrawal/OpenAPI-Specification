import csv
import random
import faker

# Initialize Faker library
fake = faker.Faker()

# Define the column headers
headers = ['id', 'name', 'email', 'date_of_birth', 'phone_number', 'address']

# Open a CSV file in write mode
with open('dummy_data.csv', 'w', newline='') as csvfile:
    writer = csv.writer(csvfile)

    # Write the headers to the CSV file
    writer.writerow(headers)

    # Generate 10 rows of dummy data
    for i in range(10):
        # Generate a unique ID
        id = i

        # Generate a random name
        name = fake.name() if random.choice([True, False]) else ""

        # Generate a random email
        email = fake.email() if random.choice([True, False]) else random.randint(1000, 99999)

        # Generate a random date of birth
        date_of_birth = fake.date_of_birth(minimum_age=22, maximum_age=90) if random.choice([True, False]) else ""

        # Generate a random phone number
        phone_number = fake.phone_number() if random.choice([True, False]) else random.randint(1000000000, 9999999999)

        # Generate a random address
        address = fake.address().replace(",", " ") if random.choice([True, False]) else ""

        # Write the data to the CSV file
        writer.writerow([id, name, email, date_of_birth, phone_number, address])
