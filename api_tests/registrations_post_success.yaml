# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/branches/{id}/registrations_post for http method type POST in artillery framework
# RoostTestHash=16a6858a84

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_post_success.csv"
    fields:
      - "id"
      - "reference"
      - "startDate"
      - "endDate"
      - "credentialUrl"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Create new Registration for the Recipient"
    flow:
      - log: "Creating new Registration for the Recipient"
      - post:
          url: "/recipients/branches/{{ id }}/registrations"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 201
            - hasProperty: "headers.api-version"
            - hasProperty: "headers.location"
            - hasProperty: "body.id"
            - hasProperty: "body.reference"
            - hasProperty: "body.startDate"
            - hasProperty: "body.endDate"
            - hasProperty: "body.status"
            - hasProperty: "body.credentialUrl"
            - hasProperty: "body.suspendedUntil"
            - hasProperty: "body.issuedAt"
            - hasProperty: "body.createdAt"
            - hasProperty: "body.updatedAt"
      - think: 1
