# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /recipients/branches/{id}/registrations_post for http method type POST in artillery framework
# RoostTestHash=16a6858a84

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_post_success.csv"
    fields:
      - "id"
      - "reference"
      - "startDate"
      - "endDate"
      - "credentialUrl"
    skipHeader: true

scenarios:
  - name: "Create new Registration for the Recipient"
    flow:
      - log: "Creating new Registration for the Recipient"
      - post:
          url: "/recipients/branches/{{ id }}/registrations"
          json:
            reference: "{{ reference }}"
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 201
            - hasProperty: "createdAt"
            - hasProperty: "credentialUrl"
            - hasProperty: "endDate"
            - hasProperty: "id"
            - hasProperty: "issuedAt"
            - hasProperty: "reference"
            - hasProperty: "startDate"
            - hasProperty: "status"
            - hasProperty: "suspendedUntil"
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
            - hasHeader: "location"
      - think: 1
