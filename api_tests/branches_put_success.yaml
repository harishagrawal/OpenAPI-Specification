# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/branches_put for http method type PUT in artillery framework
# RoostTestHash=63322ffb26

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "branches_put_success.csv"
    fields:
      - "cocNumber"
      - "cocBranchNumber"
      - "city"
      - "tradeName"
      - "phone"
      - "email"
  plugins:
    expect: {}
    
scenarios:
  - name: "Recipient Branch API Test"
    flow:
      - log: "Testing Add new branch API"
      - post:
          url: "/recipients/branches"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: "cocNumber"
            - hasProperty: "cocBranchNumber"
            - hasProperty: "city"
            - hasProperty: "tradeName"
            - hasProperty: "phone"
            - hasProperty: "email"
            - hasHeader: "api-version"
            - hasHeader: "location"
      - think: 1
