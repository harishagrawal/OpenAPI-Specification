# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /me_get for http method type GET in artillery framework
# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Account details API test"
    flow:
      - log: "Starting test for /me"
      - get:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - json: "$.id"
              as: "id"
            - json: "$.did"
              as: "did"
            - json: "$.name"
              as: "name"
            - json: "$.image"
              as: "image"
            - json: "$.email"
              as: "email"
            - json: "$.settings"
              as: "settings"
            - json: "$.createdAt"
              as: "createdAt"
            - json: "$.updatedAt"
              as: "updatedAt"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - equals:
                - "{{ id }}"
                - "{{ did }}"
                - "{{ name }}"
                - "{{ image }}"
                - "{{ email }}"
                - "{{ settings }}"
                - "{{ createdAt }}"
                - "{{ updatedAt }}"
      - think: 1
      - log: "Test for /me is successful"
