# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /me_get for http method type GET in artillery framework
# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Account Details Test Scenario"
    flow:
      - log: "Sending GET request to /me endpoint"
        get:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
        think: 1
      - log: "Sending GET request to /me endpoint with incorrect credentials"
        get:
          url: "/me"
          headers:
            Authorization: "Incorrect_Token"
          expect:
            - statusCode: 401
            - hasHeader: "api-version"
        think: 1
      - log: "Sending GET request to /me endpoint with bad request"
        get:
          url: "/me/bad_request"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
        think: 1
