# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/me_get_success.csv"
    fields:
      - "createdAt"
      - "did"
      - "email"
      - "id"
      - "image"
      - "name"
      - "settings"
      - "updatedAt"
    skipHeader: true

scenarios:
  - name: "Test scenario for retrieving my account details"
    flow:
      - comment: "Expecting successful retrieval of account details with a valid API key authorization."
        get:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Getting my account details"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "createdAt"
            - hasProperty: "email"
            - hasProperty: "image"
            - hasProperty: "name"
            - hasProperty: "settings"
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
        think: 1

      - comment: "Expecting 401 unauthorized error when API key authorization is missing."
        get:
          url: "/me"
          log: "Trying to get account details without authorization"
          expect:
            - statusCode: 401
        think: 1

      # Add more requests here if necessary to cover different scenarios based on the provided API spec.

