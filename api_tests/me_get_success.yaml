# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /me_get for http method type GET in artillery framework

# RoostTestHash=ae6b8ce5ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  ensure:
    p95: 200
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "me_get_success.csv"
    fields: ["id", "did", "name", "image", "email", "settings", "createdAt", "updatedAt"]
    skipHeader: true
  environments:
    dev:
      target: "http://dev.api.host"

scenarios:
  - name: "Test /me endpoint"
    flow:
      - get:
           url: "/me"
           headers:
             Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
           expect:
             # Check status code
             - statusCode: 200
             # Check response headers
             - hasProperty: headers.api-version
             # Check response body
             - hasProperty: body.id
             - hasProperty: body.did
             - hasProperty: body.name
             - hasProperty: body.image
             - hasProperty: body.email
             - hasProperty: body.settings
             - hasProperty: body.createdAt
             - hasProperty: body.updatedAt
           # Log statement
           afterResponse: "Log: Received account details successfully"
           think: 1
