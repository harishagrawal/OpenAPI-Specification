# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_get_success.csv"
    fields:
      - "id"
      - "did"
      - "name"
      - "email"
      - "image"
      - "createdAt"
      - "updatedAt"
      - "settings"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Account info scenario"
    flow:
      - comment: "Request account info with valid API key"
        get:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "email"
            - hasProperty: "name"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
            - hasHeader: "api-version"
        log: "Retrieving the account information with valid API key should return status 200 with required fields"
        think: 1

      - comment: "Request account info with expired API key"
        get:
          url: "/me"
          headers:
            Authorization: "Expired {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
        log: "Trying to retrieve the account information with expired API key should return status 401"
        think: 1


