# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /me_get for http method type GET in artillery framework
# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_get_success.csv"
    skipHeader: true
    fields:
      - "email"
      - "name"
      - "image"
  plugins:
    expect: {}

scenarios:
  - name: "User details retrieval scenario"
    flow:
      - comment: "Get my account details with valid authentication header"
        get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - json: "$.id"
              as: "user_id"
            - json: "$.did"
              as: "user_did"
            - json: "$.name"
              as: "user_name"
            - json: "$.image"
              as: "user_image"
            - json: "$.email"
              as: "user_email"
            - json: "$.settings"
              as: "user_settings"
            - json: "$.createdAt"
              as: "user_createdAt"
            - json: "$.updatedAt"
              as: "user_updatedAt"
          log: "Request to retrieve account details"
          think: 1
          expect:
            - statusCode: 200
            - jsonpath: "$.id"
            - jsonpath: "$.email"
            - jsonpath: "$.name"
            - header: "api-version"

