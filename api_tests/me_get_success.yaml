# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /me_get for http method type GET in artillery framework

# RoostTestHash=ae6b8ce5ab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "me_get_success.csv"
    fields:
      - "name"
      - "email"
      - "image"
      - "settings"
    skipHeader: true

scenarios:
  - name: "Test /me API endpoint"
    flow:
      - log: "Testing GET /me request"
        get:
          url: "/me"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 200
          - hasProperty: "id"
          - hasProperty: "did"
          - hasProperty: "name"
          - hasProperty: "image"
          - hasProperty: "email"
          - hasProperty: "settings"
          - hasProperty: "createdAt"
          - hasProperty: "updatedAt"
          - header: "api-version"
        think: 1
