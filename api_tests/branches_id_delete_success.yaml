# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /recipients/branches/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=1ce4bf6e3c

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "branches_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Test Recipient Branch"
    flow:
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasProperty: 'api-version'
          beforeRequest: 
            log: "Sending GET request to /recipients/branches/{{ id }}"
          think: 1

      - post:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasProperty: 'api-version'
          beforeRequest: 
            log: "Sending POST request to /recipients/branches/{{ id }}"
          think: 1

      - put:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasProperty: 'api-version'
          beforeRequest: 
            log: "Sending PUT request to /recipients/branches/{{ id }}"
          think: 1

      - delete:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasProperty: 'api-version'
          beforeRequest: 
            log: "Sending DELETE request to /recipients/branches/{{ id }}"
          think: 1
