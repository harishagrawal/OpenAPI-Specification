# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=16a6858a84

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_post_others.csv"
    fields:
      - "id"
      - "startDate"
      - "endDate"
      - "credentialUrl"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Recipient Branch Registration API tests"
    flow:
      - log: "Starting test with invalid URL"
      - post:
          url: "/recipients/branches/{id}/invalid"
          headers:
            Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1

      - log: "Test with missing required fields in payload"
      - post:
          url: "/recipients/branches/{id}/registrations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "field"
      - think: 1

      - log: "Test with invalid Authorization token"
      - post:
          url: "/recipients/branches/{id}/registrations"
          headers:
            Authorization: "{{ $processEnvironment.INVALID_AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
