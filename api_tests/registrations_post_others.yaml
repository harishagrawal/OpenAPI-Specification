# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=16a6858a84

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "registrations_post_others.csv"
    fields:
      - "id"
      - "startDate"
      - "endDate"
      - "credentialUrl"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Recipient Branch Registration API tests"
    flow:
      - log: "Starting test with invalid URL"
        post:
          url: "/invalid_url"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 404
        think: 1

      - log: "Starting test with invalid authorization"
        post:
          url: "/recipients/branches/{{ id }}/registrations"
          json:
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasProperty: "error"
        think: 1

      - log: "Starting test with invalid request body"
        post:
          url: "/recipients/branches/{{ id }}/registrations"
          json:
            startDate: "invalidDate"
            endDate: "{{ endDate }}"
            credentialUrl: "{{ credentialUrl }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
            - hasProperty: "value"
            - hasProperty: "field"
            - hasProperty: "schema_field"
        think: 1
