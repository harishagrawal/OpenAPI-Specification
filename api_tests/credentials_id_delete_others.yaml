# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=f1fc1d06f7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugin:
    - expect: {}
  payload:
    path: "/mnt/data/credentials_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "credential_failure_handling"
    flow:
      - comment: "Sending a request with a missing id, expecting 400 Bad Request"
        post:
          url: "/credentials/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with missing id"
        think: 2
        expect:
          - statusCode: 400
          - hasProperty: "description"
          - hasProperty: "error"

      - comment: "Sending a request with an invalid id format, expecting 400 Bad Request"
        post:
          url: "/credentials/invalid-id-format"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with invalid id format"
        think: 2
        expect:
          - statusCode: 400
          - hasProperty: "description"
          - hasProperty: "error"
          - hasProperty: "schema_field"
          - hasProperty: "value"

      - comment: "Sending a request to a non-existing endpoint to simulate Wrong URL, expecting 400 Bad Request"
        post:
          url: "/credntials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with a non-existing endpoint"
        think: 2
        expect:
          - statusCode: 400
          - hasProperty: "description"
          - hasProperty: "error"

      - comment: "Sending a request with an invalid authorization, expecting 401 Unauthorized"
        post:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with invalid authorization"
        think: 2
        expect:
          - statusCode: 401
          - hasProperty: "description"
          - hasProperty: "error"

