# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=f1fc1d06f7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_id_delete_others.csv"
    fields: ["id"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - flow:
    - log: "Delete one credential from my Wallet - Invalid UUID"
      delete:
        url: "/credentials/invalid"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "headers.api-version"
          - hasProperty: "body.error"
          - hasProperty: "body.description"
        # Expecting a 400 status code as the UUID provided is invalid
      think: 5
    - log: "Delete one credential from my Wallet - Unauthorized"
      delete:
        url: "/credentials/{{ id }}"
        headers:
          Authorization: "invalid token"
        expect:
          - statusCode: 401
          - hasProperty: "headers.api-version"
          - hasProperty: "body.error"
          - hasProperty: "body.description"
        # Expecting a 401 status code as the authorization token is invalid
      think: 5
    - log: "Delete one credential from my Wallet - Invalid URL"
      delete:
        url: "/invalid/{{ id }}"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        # Expecting a 404 status code as the URL is invalid
      think: 5
