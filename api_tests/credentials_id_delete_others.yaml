# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=f1fc1d06f7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Delete one credential from my Wallet"
    flow:
      - log: "Delete credential with invalid id"
        delete:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: 'headers.api-version'
            - hasProperty: 'body.error'
            - hasProperty: 'body.description'
      - think: 1
      - log: "Delete credential with invalid URL"
        delete:
          url: "/invalid_url/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Delete credential with invalid Authorization"
        delete:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Invalid_Auth_Token"
          expect:
            - statusCode: 401
            - hasProperty: 'headers.api-version'
            - hasProperty: 'body.error'
            - hasProperty: 'body.description'
