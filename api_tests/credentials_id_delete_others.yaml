# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=f1fc1d06f7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_id_delete_others.csv"
    fields: ["id"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - flow:
    - log: "Invalid URL Test"
    - get:
        url: "/invalid-url/{{ id }}"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
          - hasProperty: "description"
          - hasProperty: "error"
        # Comment: This request is expected to fail as the URL is invalid
    - think: 1
    - log: "Invalid Authorization Test"
    - get:
        url: "/credentials/{{ id }}"
        headers:
          Authorization: "InvalidToken"
        expect:
          - statusCode: 401
          - hasProperty: "description"
          - hasProperty: "error"
          - hasHeader: "api-version"
        # Comment: This request is expected to fail as the Authorization token is invalid
    - think: 1
    - log: "Invalid ID Test"
    - get:
        url: "/credentials/invalid-id"
        headers:
          Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "description"
          - hasProperty: "error"
          - hasHeader: "api-version"
        # Comment: This request is expected to fail as the UUID in the path is invalid
