# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=f1fc1d06f7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/path/to/credentials_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - flow:
      - log: "Invalid ID - Passing an invalid UUID"
      - delete:
          url: "/credentials/1234"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasProperty: 'description'

      - think: 2

      - log: "Unauthorized - Sending request without authentication token"
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Bearer wrongtoken"
          expect:
            - statusCode: 401
            - hasProperty: 'error'
            - hasProperty: 'description'
            
      - think: 2

      - log: "Invalid URL - Sending request to a non-existing endpoint"
      - delete:
          url: "/credential/invalid-endpoint"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404 # Assuming that 404 is a standard response for non-existing endpoints

