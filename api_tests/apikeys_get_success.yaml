# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /apikeys_get for http method type GET in artillery framework
# RoostTestHash=9e57a3b0dd

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_get_success.csv"
    fields: ["apikey"]
    skipHeader: true
scenarios:
  - name: "List API Keys"
    flow:
      - log: "Send GET request to /apikeys"
      - get:
          url: "/apikeys"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "apikeys"
            - hasHeader: "api-version"
      - think: 1
      - log: "GET request to /apikeys was successful"
