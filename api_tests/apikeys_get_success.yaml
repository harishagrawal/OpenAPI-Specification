# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /apikeys_get for http method type GET in artillery framework
# RoostTestHash=9e57a3b0dd

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "API keys test"
    flow:
      - log: "Starting API keys test"
      - get:
          url: "/apikeys"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - json: "$.total"
              as: "total"
            - json: "$.apikeys[0].id"
              as: "id"
            - json: "$.apikeys[0].createdAt"
              as: "createdAt"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "apikeys"
            - hasHeader: "api-version"
      - log: "API keys test successful"
      - think: 1
