# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/persons_get for http method type GET in artillery framework
# RoostTestHash=aa0a5d4980

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "./persons_get_success.csv"
    fields:
      - "size"
      - "offset"
      - "name"
      - "birthDate"
    skipHeader: true

scenarios:
  - name: "Test Recipient Person API"
    flow:
      - log: "Testing GET /recipients/persons with valid size and offset"
        get:
          url: "/recipients/persons"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            size: "{{ size }}"
            offset: "{{ offset }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "persons"
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
        think: 1
      - log: "Testing GET /recipients/persons with valid name"
        get:
          url: "/recipients/persons"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            name: "{{ name }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "persons"
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
        think: 1
      - log: "Testing GET /recipients/persons with valid birth date"
        get:
          url: "/recipients/persons"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            birthDate: "{{ birthDate }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "persons"
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
        think: 1
