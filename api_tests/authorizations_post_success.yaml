# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /authorizations_post for http method type POST in artillery framework
# RoostTestHash=ed8c1b7bff

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "authorizations_post_success.csv"
    fields:
      - "email"
    skipHeader: true

scenarios:
  - name: "Authorize another existing Account to access my Account"
    flow:
      - post:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: 'headers.api-version'
            - hasProperty: 'headers.location'
            - hasProperty: 'body.id'
            - hasProperty: 'body.name'
            - hasProperty: 'body.email'
            - hasProperty: 'body.createdAt'
            - hasProperty: 'body.updatedAt'
          log: "Authorization successful"
        think: 1
