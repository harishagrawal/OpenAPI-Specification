# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /recipients/persons/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=819791060f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "persons_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Test /recipients/persons/{id} endpoint"
    flow:
      - log: "Making a request to /recipients/persons/{id}"
      - get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "headers.api-version"
            - hasProperty: "description"
          afterResponse: "think"
      - log: "Request to /recipients/persons/{id} was successful"
      - think: 1
