# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /recipients/persons/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=819791060f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "persons_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Test the /recipients/persons/{id} endpoint"
    flow:
      - log: "Making a request to the /recipients/persons/{id} endpoint"
        get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - bodyContains:
                - "{{ id }}"
        think: 1
      - log: "Making another request to the /recipients/persons/{id} endpoint"
        get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - bodyContains:
                - "{{ id }}"
        think: 1
