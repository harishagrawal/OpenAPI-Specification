import csv
import random
import faker

# Initialize faker generator
fake = faker.Faker()

# Define CSV file name
csv_file = "dummy_data.csv"

# Define fieldnames (columns)
fields = ["id", "name", "email", "dob", "phone"]

# Generate dummy values
def generate_dummy_values():
    id = random.randint(1000, 9999)
    name = fake.name().replace(",", "")
    email = fake.email()
    dob = fake.date_of_birth(minimum_age=20, maximum_age=60).isoformat()
    phone = fake.phone_number().replace(",", "")

    return {"id": id, "name": name, "email": email, "dob": dob, "phone": phone}

# Write data to CSV file
with open(csv_file, mode='w', newline='') as file:
    writer = csv.DictWriter(file, fieldnames=fields)
    writer.writeheader()
    for _ in range(10):  # Generate 10 rows of data
        writer.writerow(generate_dummy_values())
