# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=2758affb7e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_patch_others.csv"
    fields: 
      - "email"
    skipHeader: true

scenarios:
  - name: "Failure cases, edge cases, and error handling for /accounts API"
    flow:
      - log: "Sending a request with invalid email format"
        post:
          url: "/accounts"
          json:
            email: "invalidemail"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - equals: 
              - "error"
              - "validation/email-pattern"
            - hasProperty: "headers.api-version"
        think: 1

      - log: "Sending a request with missing email field"
        post:
          url: "/accounts"
          json: {}
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - equals: 
              - "error"
              - "error/code"
            - hasProperty: "headers.api-version"
        think: 1

      - log: "Sending a request with invalid authorization token"
        post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "headers.api-version"
        think: 1

      - log: "Sending a request to invalid URL"
        post:
          url: "/invalidURL"
          json:
            email: "{{ email }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
