# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=2758affb7e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/accounts_patch_others.csv"
    fields:
      - "invalidEmail"
      - "missingEmail"
    skipHeader: true
  plugin:
    expect: {}

scenarios:
  - name: "Negative tests for /accounts API endpoint"
    flow:
      - log: "Sending an invalid email format to test the validation error response"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ invalidEmail }}"
          expect:
            - statusCode: 400
            - header: "api-version"
            - hasProperty: "description"
            - hasProperty: "error"
            - hasProperty: "schema_field"
            - hasProperty: "value"
        # Expected behavior: Should return 400 Bad Request with validation error for email pattern.
      
      - think: 1

      - log: "Omitting the email field to test the missing required field error response"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ missingEmail }}"
          expect:
            - statusCode: 400
            - header: "api-version"
            - hasProperty: "description"
            - hasProperty: "error"
        # Expected behavior: Should return 400 Bad Request with error indicating required field email is missing.
      
      - think: 1

      - log: "Providing an invalid URL to test the endpoint not found error"
      - post:
          url: "/invalid_accounts" # Invalid URL to simulate the failure case.
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ invalidEmail }}"
          expect:
            - statusCode: 404 # Assuming the API will return 404 for not found URLs.
        # Expected behavior: Should return 404 Not Found as the URL is invalid.
      
      - think: 1

      - log: "Using an invalid Authorization token to test the unauthorized error response"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}" # Invalid token to test authorization failure.
          json:
            email: "{{ invalidEmail }}"
          expect:
            - statusCode: 401
            - header: "api-version"
        # Expected behavior: Should return 401 Unauthorized due to the invalid authentication token.

