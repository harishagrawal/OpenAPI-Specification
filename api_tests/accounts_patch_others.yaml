# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=2758affb7e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "accounts_patch_others.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "scenario"
    flow:
      # Email presented in an invalid format (missing '@')
      - think: 1
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            Content-Type: "application/json"
          log: "Intensionally passing an invalid email format to receive a Bad Request (400) error"
        # Expected behavior: The server should respond with a 400 status code due to invalid email format
      - expect:
          - statusCode: 400
          - hasProperty:
              name: "api-version"
              type: "string"
      - expect:
          - statusCode: 400
          - hasProperty:
              name: "error"

      # Email field missing from payload
      - think: 1
      - post:
          url: "/accounts"
          json: {}
          headers:
            Content-Type: "application/json"
          log: "Sending a request without the mandatory email field to trigger a Bad Request (400) error"
        # Expected behavior: The server should respond with a 400 status code due to missing required 'email' field
      - expect:
          - statusCode: 400
          - hasProperty:
              name: "api-version"
              type: "string"
      - expect:
          - statusCode: 400
          - hasProperty:
              name: "error"

      # Validation failure due to incorrect URL
      - think: 1
      - post:
          url: "/acounts"  # invalid URL
          json:
            email: "{{ email }}"
          headers:
            Content-Type: "application/json"
          log: "Sending to an invalid URL endpoint to trigger a failure"
        # Expected behavior: The server should not find the endpoint, resulting in a 404 error
      - expect:
          - statusCode: 404
          - hasProperty:
              name: "api-version"
              type: "string"
      - expect:
          - statusCode: 404
          - hasProperty:
              name: "error"

      # Authorization failure due to invalid token
      - think: 1
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            Content-Type: "application/json"
            Authorization: "invalid_token"
          log: "Testing with invalid authorization token to simulate auth failure"
        # Expected behavior: The server should respond with a 401 Unauthorized status code due to invalid token
      - expect:
          - statusCode: 401
          - hasProperty:
              name: "api-version"
              type: "string"
      - expect:
          - statusCode: 401
          - hasProperty:
              name: "error"

