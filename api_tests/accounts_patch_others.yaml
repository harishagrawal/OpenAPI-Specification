# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=2758affb7e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_patch_others.csv"
    fields: ["email"]
    skipHeader: true

scenarios:
  - name: "API failure and edge cases"
    flow:
      - post:
          url: "/accounts"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          log: "Send POST request with valid email"
          expect:
            - statusCode: 400
            - hasProperty: "api-version"
            - hasProperty: "description"
            - hasProperty: "error"
      - think: 1
      - post:
          url: "/accounts"
          headers:
            Authorization: "Invalid Auth"
          json:
            email: "{{ email }}"
          log: "Send POST request with invalid authorization"
          expect:
            - statusCode: 401
      - think: 1
      - post:
          url: "/invalid_url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          log: "Send POST request to invalid url"
          expect:
            - statusCode: 404
