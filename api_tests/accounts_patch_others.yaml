# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# RoostTestHash=2758affb7e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "accounts_patch_others.csv"
    fields:
      - "email"
    skipHeader: true

scenarios:
  - name: "Test non-success API responses"
    flow:
      - log: "Sending a request with an invalid email"
        post:
          url: "/accounts"
          json:
            email: "invalidEmail"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            # Expect an error message indicating email validation failure
      - think: 1

      - log: "Sending a request without an email"
        post:
          url: "/accounts"
          json: {}
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            # Expect an error message indicating missing required fields
      - think: 1

      - log: "Sending a request with an invalid token"
        post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            Authorization: "Bearer InvalidToken"
          expect:
            - statusCode: 401
            # Expect an unauthorized status code due to invalid token
      - think: 1

      - log: "Sending a request to an invalid URL"
        post:
          url: "/invalidUrl"
          json:
            email: "{{ email }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            # Expect a not found status code due to invalid URL
