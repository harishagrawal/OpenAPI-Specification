# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=2758affb7e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_patch_others.csv"
    fields: ["email"]
    skipHeader: true

scenarios:
  - name: "API failure cases, edge cases, and error handling"
    flow:
      - log: "Invalid URL"
        post:
          url: "/invalid-url"
          headers:
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Invalid email payload"
        post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
          json:
            email: "invalid-email"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
      - think: 1
      - log: "Missing email in payload"
        post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
          json: {}
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
      - think: 1
      - log: "Invalid Authorization"
        post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Missing Authorization"
        post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 403
