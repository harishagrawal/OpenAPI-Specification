# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=692c6b853f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "grants_get_others.csv"
    fields:
      - "email"
      - "name"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "API robustness testing"
    flow:
      - log: "Sending a request with invalid URL"
        get:
          url: "/invalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 5
      - log: "Sending a request with invalid authorization token"
        get:
          url: "/grants"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "headers.api-version"
      - think: 5
      - log: "Sending a request with valid authorization token but without required headers"
        get:
          url: "/grants"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 5
      - log: "Sending a request with valid authorization token and required headers"
        get:
          url: "/grants"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "grants"
            - hasProperty: "headers.api-version"
