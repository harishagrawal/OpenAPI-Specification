# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=692c6b853f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "grants_get_others.csv"
    fields:
      - "email"
      - "name"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: "API robustness testing"
    flow:
      - log: "Sending a request with valid headers but incorrect authorization"
        get:
          url: "/grants"
          headers:
            Authorization: "Bearer InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: 'error'
            - hasProperty: 'description'
      - think: 1
      - log: "Sending a request with invalid URL"
        get:
          url: "/invalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Sending a request with valid headers and authorization but incorrect payload data"
        get:
          url: "/grants"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            name: "{{ name }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasProperty: 'description'
      - think: 1
