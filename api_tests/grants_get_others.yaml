# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=692c6b853f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "grants_get_others.csv"
    fields:
      - "email"
      - "name"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 10
scenarios:
  - name: "API robustness testing"
    flow:
      - log: "Sending invalid request without authorization"
        get:
          url: "/grants"
          expect:
            - statusCode: 401
            - hasProperty: "headers.api-version"
            - hasProperty: "body.error"
            - hasProperty: "body.description"
        think: 1
      - log: "Sending request with invalid authorization"
        get:
          url: "/grants"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "headers.api-version"
            - hasProperty: "body.error"
            - hasProperty: "body.description"
        think: 1
      - log: "Sending request to invalid URL"
        get:
          url: "/invalid-url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Sending request with invalid payload"
        post:
          url: "/grants"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            name: "InvalidName"
          expect:
            - statusCode: 400
            - hasProperty: "headers.api-version"
            - hasProperty: "body.error"
            - hasProperty: "body.description"
            - hasProperty: "body.schema_field"
            - hasProperty: "body.value"
            - hasProperty: "body.field"
        think: 1
