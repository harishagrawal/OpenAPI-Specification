# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=819791060f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "persons_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Test scenario for /recipients/persons/{id} API"
    flow:
      - log: "Sending a request with invalid id"
        get:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "description"
        think: 5
      - log: "Sending a request without authorization token"
        get:
          url: "/recipients/persons/{{ id }}"
        expect:
          - statusCode: 401
          - hasProperty: "error"
          - hasProperty: "description"
        think: 5
      - log: "Sending a request to invalid URL"
        get:
          url: "/recipients/persons/invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 5
