# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=9e57a3b0dd

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "apikeys_get_others.csv"
    fields:
      - "apikey"
    skipHeader: true
scenarios:
  - name: "API Keys Failure Test Cases"
    flow:
      - log: "Sending a request with invalid URL"
        get:
          url: "/invalidurl"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 5
      - log: "Sending a request without Authorization header"
        get:
          url: "/apikeys"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
      - think: 5
      - log: "Sending a request with invalid Authorization token"
        get:
          url: "/apikeys"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
      - think: 5
      - log: "Sending a request with valid Authorization token but invalid apikey in the payload"
        get:
          url: "/apikeys"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            apikey: "InvalidKey"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
