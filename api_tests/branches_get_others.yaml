# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=74a40da00e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "branches_get_others.csv"
    fields:
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
scenarios:
  - name: "API Test Scenario"
    flow:
      - log: "Invalid URL test"
      - think: 1
      - get:
          url: "/invalid_url"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - log: "Invalid parameters test"
      - think: 1
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}&coc-branch-number={{ coc-branch-number }}&trade-name={{ trade-name }}&size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - log: "Unauthorized access test"
      - think: 1
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}&coc-branch-number={{ coc-branch-number }}&trade-name={{ trade-name }}&size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
