# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=74a40da00e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "branches_get_others.csv"
    fields: 
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
scenarios:
  - name: "Test /recipients/branches API endpoint"
    flow:
      - log: "Testing with invalid URL"
        get:
          url: "/invalidurl"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Testing with invalid authorization token"
        get:
          url: "/recipients/branches"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing with invalid coc-number"
        get:
          url: "/recipients/branches"
          qs:
            coc-number: "invalid_coc_number"
            coc-branch-number: "{{ coc-branch-number }}"
            trade-name: "{{ trade-name }}"
            size: "{{ size }}"
            offset: "{{ offset }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Testing with invalid coc-branch-number"
        get:
          url: "/recipients/branches"
          qs:
            coc-number: "{{ coc-number }}"
            coc-branch-number: "invalid_coc_branch_number"
            trade-name: "{{ trade-name }}"
            size: "{{ size }}"
            offset: "{{ offset }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Testing with invalid size"
        get:
          url: "/recipients/branches"
          qs:
            coc-number: "{{ coc-number }}"
            coc-branch-number: "{{ coc-branch-number }}"
            trade-name: "{{ trade-name }}"
            size: 100
            offset: "{{ offset }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Testing with invalid offset"
        get:
          url: "/recipients/branches"
          qs:
            coc-number: "{{ coc-number }}"
            coc-branch-number: "{{ coc-branch-number }}"
            trade-name: "{{ trade-name }}"
            size: "{{ size }}"
            offset: -1
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
