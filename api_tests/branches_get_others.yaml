# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=74a40da00e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "branches_get_others.csv"
    fields: 
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
scenarios:
  - name: "Test failure, edge and error cases for /recipients/branches API"
    flow:
      - log: "Test Case: Invalid URL"
        get:
          url: "/invalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Test Case: Invalid Authorization"
        get:
          url: "/recipients/branches"
          headers:
            Authorization: "Invalid_Auth_Token"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Test Case: Invalid Headers"
        get:
          url: "/recipients/branches"
          headers:
            InvalidHeader: "InvalidHeaderValue"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Test Case: Invalid Query Params"
        get:
          url: "/recipients/branches"
          qs:
            invalidQueryParam: "invalidValue"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Test Case: Exceed max limit in size param"
        get:
          url: "/recipients/branches"
          qs:
            size: 51
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Test Case: Negative value in offset param"
        get:
          url: "/recipients/branches"
          qs:
            offset: -1
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Test Case: Invalid coc-number format"
        get:
          url: "/recipients/branches"
          qs:
            coc-number: "invalidCOCNumber"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Test Case: Invalid coc-branch-number format"
        get:
          url: "/recipients/branches"
          qs:
            coc-branch-number: "invalidCOCBranchNumber"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
