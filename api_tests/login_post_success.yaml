# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "login_post_success.csv"
    fields:
      - "email"
      - "otp"
      - "client"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Successful Login using OTP"
    flow:
      - log: "Starting Login using OTP"
      - post:
          url: "/login"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 201
            - hasProperty: "token"
            - hasHeader: "api-version"
          think: 1
      - log: "Login using OTP Successful"
