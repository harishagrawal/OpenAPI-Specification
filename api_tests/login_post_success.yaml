# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=e0cf5b2e0e

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "login_post_success.csv"
    fields:
      - "email"
      - "otp"
      - "client"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Login scenario"
    flow:
      - post:
          url: "/login"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            - json: "$.token"
              as: "session_token"
          expect:
            - statusCode: 201
            - hasProperty: "token"
            - hasHeader: "api-version"
          log: "Login successful and session token received."
      - think: 1
      - post:
          url: "/login"
          json:
            email: "{{ email }}"
            otp: "wrong_otp"
            client: "{{ client }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
          log: "Login failed with incorrect OTP as expected."
      - think: 1
