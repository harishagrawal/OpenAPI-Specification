# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3122292ee3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/login_post_success.csv"
    fields:
      - "email"
      - "otp"
      - "client"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful login with OTP"
    flow:
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          log: "Trying to login with OTP"
          # Successful operation
          think: 1
          expect:
            - statusCode: 201
            - hasProperty: "token"
            - hasHeader: "api-version"

      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "api"
          log: "Trying to login with OTP from api client"
          # Successful operation from alternative client
          think: 1
          expect:
            - statusCode: 201
            - hasProperty: "token"
            - hasHeader: "api-version"

