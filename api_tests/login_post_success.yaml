# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=3122292ee3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/login_post_success.csv"
    fields:
      - "client"
      - "email"
      - "otp"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Login scenario"
    flow:
      - post:
          url: "/login"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Logging in with mobile client and valid credentials"
          think: 1
          # Expect 201 success and validate api-version header
          expect:
            - statusCode: 201
            - hasProperty: "token"
            - hasHeader: "api-version"
      
      - post:
          url: "/login"
          json:
            email: "{{ email }}"
            otp: "wrongotp"
            client: "{{ client }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Logging in with an invalid OTP, expecting a Bad Request (400)"
          think: 1
          # Expect 400 error and check error structure for invalid OTP
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      
      - post:
          url: "/login"
          json:
            email: "invalidemail.com"
            otp: "{{ otp }}"
            client: "{{ client }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Logging in with invalid email format, expecting a Bad Request (400)"
          think: 1
          # Expect 400 error and check error structure for invalid email
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      
      - post:
          url: "/login"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "unknown"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Logging in with an unsupported client type, expecting a Bad Request (400)"
          think: 1
          # Expect 400 error and check error structure for unsupported client type
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"

