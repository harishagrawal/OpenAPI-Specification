# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=3122292ee3

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/login_post_success.csv"
    fields:
      - "email"
      - "otp"
      - "client"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Login API Test Scenario"
    flow:
      - post:
          url: "/login"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 201
            - hasProperty: "token"
          log: "Test login: Valid email, otp, and client"
          think: 1
      - post:
          url: "/login"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "wrong-format"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 400
          log: "Test login: Invalid email format"
          think: 1
      - post:
          url: "/login"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            otp: "wrong-otp"
            client: "{{ client }}"
          expect:
            - statusCode: 400
          log: "Test login: Invalid OTP format"
          think: 1
      - post:
          url: "/login"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
            otp: "{{ otp }}"
            client: "invalid-client"
          expect:
            - statusCode: 400
          log: "Test login: Invalid client value"
          think: 1

