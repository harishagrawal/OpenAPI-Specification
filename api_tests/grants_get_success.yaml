# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /grants_get for http method type GET in artillery framework
# RoostTestHash=692c6b853f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./processor.js"
  plugins:
    expect: {}
  payload:
    path: "grants_get_success.csv"
    fields:
      - "id"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Grants API test"
    flow:
      - log: "Making a successful GET request to /grants"
        get:
          url: "/grants"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: 'grants'
            - hasProperty: 'total'
            - hasHeader: 'api-version'
            - equals:
                - "{{ response.headers['api-version'] }}"
                - "1.0.0"
        think: 1
      - log: "Making an unsuccessful GET request to /grants"
        get:
          url: "/grants"
          headers:
            Authorization: "invalid token"
          expect:
            - statusCode: 401
            - hasHeader: 'api-version'
            - equals:
                - "{{ response.headers['api-version'] }}"
                - "1.0.0"
        think: 1
