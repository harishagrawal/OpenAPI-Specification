# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /grants_get for http method type GET in artillery framework
# RoostTestHash=692c6b853f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./processor.js"
  plugins:
    expect: {}
  payload:
    path: "grants_get_success.csv"
    fields:
      - "id"
      - "token"
      - "createdAt"
      - "updatedAt"
      - "email"
      - "name"
      - "image"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5
scenarios:
  - name: "Grants API test"
    flow:
      - log: "Starting test for Grants API"
      - get:
          url: "/grants"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - header: "api-version"
            - hasProperty: "grants"
            - hasProperty: "total"
            - hasProperty: "grants[0].id"
            - hasProperty: "grants[0].token"
            - hasProperty: "grants[0].createdAt"
            - hasProperty: "grants[0].updatedAt"
            - hasProperty: "grants[0].account.id"
            - hasProperty: "grants[0].account.name"
            - hasProperty: "grants[0].account.image"
            - hasProperty: "grants[0].account.email"
          capture:
            - json: "$.grants[0].id"
              as: "grantId"
            - json: "$.grants[0].token"
              as: "grantToken"
            - json: "$.grants[0].createdAt"
              as: "grantCreatedAt"
            - json: "$.grants[0].updatedAt"
              as: "grantUpdatedAt"
            - json: "$.grants[0].account.id"
              as: "accountId"
            - json: "$.grants[0].account.name"
              as: "accountName"
            - json: "$.grants[0].account.image"
              as: "accountImage"
            - json: "$.grants[0].account.email"
              as: "accountEmail"
      - think: 1
      - log: "Test for Grants API completed successfully"
