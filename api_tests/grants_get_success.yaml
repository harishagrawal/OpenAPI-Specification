# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /grants_get for http method type GET in artillery framework
# RoostTestHash=692c6b853f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/grants_get_success.csv"
    skipHeader: true
    fields:
      - "grantId"
      - "tokenId"
      - "createdAt"
      - "updatedAt"
      - "accountId"
      - "name"
      - "email"
      - "image"
  plugins:
    expect: {}

scenarios:
  - name: "Test scenario for retrieving accounts with granted access permissions"
    flow:
      # First valid request to retrieve accounts with granted access permissions
      - get:
          url: "/grants"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "grants"
            - hasProperty: "total"
          log: "Request to retrieve accounts with granted access permissions should return a 200 status with 'grants' and 'total' in the body."
        think: 1
        
      # Add more valid requests if necessary here, using values from payload CSV for dynamically changing values
      # Remember to include 'think' and 'log' in each request, as well as proper response expectation

