# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=692c6b853f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/grants_get_success.csv"
    fields:
      - "userId"
      - "authToken"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Get Grants"
    flow:
      - comment: "Retrieve accounts with granted access permissions, expecting success"
        post:
          url: "/grants"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "api-version"
            - hasProperty: "grants"
            - hasProperty: "total"
          log: "Access grants retrieval successful"
        think: 1

      - comment: "Attempt to access without authentication, expecting failure"
        post:
          url: "/grants"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "api-version"
          log: "Authentication is required and absence confirmed"
        think: 1

