# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /grants_get for http method type GET in artillery framework
# RoostTestHash=692c6b853f

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./processor.js"
  payload:
    path: "./grants_get_success.csv"
    fields:
      - "id"
      - "token"
      - "createdAt"
      - "updatedAt"
      - "accountId"
      - "accountName"
      - "accountImage"
      - "accountEmail"
    skipHeader: true
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 1

scenarios:
  - name: "Retrieve accounts with granted access permissions"
    flow:
      - log: "Starting scenario to retrieve grants"
      - get:
          url: "/grants"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "grants"
            - hasHeader: "api-version"
            - equals:
                - "{{ api-version }}"
                - "1.0.0"
            - equals:
                - "{{ total }}"
                - 1
            - equals:
                - "{{ grants[0].id }}"
                - "{{ id }}"
            - equals:
                - "{{ grants[0].token }}"
                - "{{ token }}"
            - equals:
                - "{{ grants[0].createdAt }}"
                - "{{ createdAt }}"
            - equals:
                - "{{ grants[0].updatedAt }}"
                - "{{ updatedAt }}"
            - equals:
                - "{{ grants[0].account.id }}"
                - "{{ accountId }}"
            - equals:
                - "{{ grants[0].account.name }}"
                - "{{ accountName }}"
            - equals:
                - "{{ grants[0].account.image }}"
                - "{{ accountImage }}"
            - equals:
                - "{{ grants[0].account.email }}"
                - "{{ accountEmail }}"
      - think: 1
