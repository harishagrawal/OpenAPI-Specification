# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework
# RoostTestHash=49b35fa9c1

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "registrations_get_success.csv"
    fields:
      - "id"
      - "credential-url"
      - "size"
      - "offset"
  processor: "./myProcessor.js"

scenarios:
  - name: "Test Recipient Branch API"
    flow:
      - get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url={{ credential-url }}&size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "registrations"
            - header: "api-version"
            - header: "page-size"
            - header: "page-offset"
          log: "Received successful response for GET request"
        think: 1
      - get:
          url: "/recipients/branches/{{ id }}/registrations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "registrations"
            - header: "api-version"
            - header: "page-size"
            - header: "page-offset"
          log: "Received successful response for GET request without query parameters"
        think: 1
