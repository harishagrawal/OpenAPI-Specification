# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /recipients/branches/{id}/registrations_get for http method type GET in artillery framework
# RoostTestHash=49b35fa9c1

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "registrations_get_success.csv"
    fields:
      - "id"
      - "credential-url"
      - "size"
      - "offset"
  processor: "./myProcessor.js"

scenarios:
  - name: "Test API endpoint /recipients/branches/{id}/registrations"
    flow:
      - log: "Starting test for /recipients/branches/{id}/registrations"
      - get:
          url: "/recipients/branches/{{ id }}/registrations?credential-url={{ credential-url }}&size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "registrations"
            - hasHeader: "api-version"
            - hasHeader: "page-size"
            - hasHeader: "page-offset"
      - think: 1
