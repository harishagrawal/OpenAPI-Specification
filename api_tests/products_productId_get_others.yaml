# Test generated by RoostGPT for test mixtral-8x-api using AI Type Open Source AI and AI Model meta-llama/Llama-2-13b-chat


# Test generated for /products/{productId}_get for http method type GET in artillery framework

# RoostTestHash=ab8efa24ed


config:
  target: "http://{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 30
      arrivalRate: 10
  plugins:
    expect:
      enabled: true
  variables:
    - key: productId
      values: ["1", "2", "3"]
    - key: authToken
      values: ["token1", "token2", "token3"]
  payload:
    products_productId_get_others.csv:
      source: products_productId_get_others.csv
      skipHeader: true

scenarios:
  - flow:
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ authToken }}"
          thinkTime: 1
          expect:
            - statusCode: 200
              contentType: "application/json"
              hasProperty: "productId"
      - get:
          url: "/products/invalid"
          headers:
            Authorization: "Bearer {{ authToken }}"
          thinkTime: 1
          expect:
            - statusCode: 404
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer invalid"
          thinkTime: 1
          expect:
            - statusCode: 401
      - get:
          url: "/products/{{ productId }}"
          headers:
            Authorization: "Bearer {{ authToken }}"
          thinkTime: 1
          expect:
            - statusCode: 200
              contentType: "application/json"
              hasProperty: "productId"
