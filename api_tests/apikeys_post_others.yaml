# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=28a54f1579

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_post_others.csv"
    fields:
      - "id"
      - "token"
      - "createdAt"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "API keys failure test cases"
    flow:
      - log: "Sending a POST request to /apikeys endpoint without authorization header"
        post:
          url: "/apikeys"
          headers:
            api-version: "1.0.0"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
      - log: "Sending a POST request to /apikeys endpoint with invalid authorization header"
        post:
          url: "/apikeys"
          headers:
            api-version: "1.0.0"
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
      - log: "Sending a POST request to /apikeys endpoint with empty payload"
        post:
          url: "/apikeys"
          headers:
            api-version: "1.0.0"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
      - log: "Sending a POST request to invalid /apikeys endpoint"
        post:
          url: "/invalid/apikeys"
          headers:
            api-version: "1.0.0"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
