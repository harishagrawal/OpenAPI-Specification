# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=28a54f1579

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_post_others.csv"
    fields:
      - "id"
      - "token"
      - "createdAt"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "API keys failure cases"
    flow:
      - log: "Sending a POST request with invalid URL"
      - post:
          url: "/invalidurl"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1

      - log: "Sending a POST request with invalid Authorization token"
      - post:
          url: "/apikeys"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1

      - log: "Sending a POST request with missing required fields in the payload"
      - post:
          url: "/apikeys"
          json:
            id: "{{ id }}"
            token: ""
            createdAt: "{{ createdAt }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1

      - log: "Sending a POST request with invalid data format in the payload"
      - post:
          url: "/apikeys"
          json:
            id: "invalidformat"
            token: "{{ token }}"
            createdAt: "{{ createdAt }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
