# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=018c225aea

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_success.csv"
    fields:
      - "id"
      - "format"
      - "credential"
      - "status"
      - "statusUpdatedAt"
      - "createdAt"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test Credentials API Endpoint"
    flow:
      - comment: "Request a valid credential using a UUID from the payload file"
        get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasProperty: 'createdAt'
            - hasProperty: 'credential'
            - hasProperty: 'format'
            - hasProperty: 'id'
            - hasProperty: 'status'
            - hasProperty: 'statusUpdatedAt'
            - header: 'api-version'
          log: "Valid credential requested"
        think: 1

      - comment: "Testing credential with another valid UUID format"
        get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasProperty: 'createdAt'
            - hasProperty: 'credential'
            - hasProperty: 'format'
            - hasProperty: 'id'
            - hasProperty: 'status'
            - hasProperty: 'statusUpdatedAt'
            - header: 'api-version'
          log: "Second valid credential tested"
        think: 1

      - comment: "Attempting to fetch a credential with an inactive or invalid status to check 200 response"
        get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasProperty: 'createdAt'
            - hasProperty: 'credential'
            - hasProperty: 'format'
            - hasProperty: 'id'
            - hasProperty: 'status'
            - hasProperty: 'statusUpdatedAt'
            - header: 'api-version'
          log: "Credential with inactive/invalid status tested for a valid response"
        think: 1

