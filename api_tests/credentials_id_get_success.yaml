# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=018c225aea

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/credentials_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Get Credential by ID"
    flow:
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "format"
            - hasProperty: "credential"
            - hasProperty: "status"
            - hasProperty: "statusUpdatedAt"
            - hasProperty: "createdAt"
            - hasHeader: "api-version"
          log: "Getting credential by ID should return credential details with status 200 and the specified headers and body properties."
      - think: 1
      
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "format"
            - hasPropertyValue: { name: "status", value: "active" }
            - hasHeader: "api-version"
          log: "Credentials returned should include an active status, appropriate format, and the expected API version header."
      - think: 1

      - get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "statusUpdatedAt"
            - hasProperty: "createdAt"
            - hasHeader: "api-version"
          log: "Checking that the credential includes timestamps for creation and last status update in the right format."
      - think: 1

