# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=018c225aea

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/credentials_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Get single credential by id from Wallet"
    flow:
      # Retrieve a specific credential by id
      - get:
          url: "/credentials/{{ id }}"
          log: "Requesting a specific credential by id."
          think: 1
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "format"
            - hasProperty: "credential"
            - hasProperty: "status"
            - hasProperty: "statusUpdatedAt"
            - hasProperty: "createdAt"
            - hasHeader: "api-version"
      - log: "Credential retrieval successful."

