# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_get_others.csv"
    fields:
      - "did"
      - "value"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - flow:
      - log: "Requesting with an invalid URL to test the server's response to unrecognized paths."
      - get:
          url: "/me-invalid"
          # there's no need to set payload or headers as we're simulating an invalid url request
          expect:
            - statusCode: 400
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

      - think: 1

      - log: "Requesting with valid URL but without authorization header to induce 401 Unauthorized response."
      - get:
          url: "/me"
          # no headers added here to induce a missing Authorization header
          expect:
            - statusCode: 401

      - think: 1

      - log: "Sending request with invalid Authorization header to induce 401 Unauthorized response."
      - get:
          url: "/me"
          expect:
            - statusCode: 401
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"

      - think: 1
      
      - log: "Sending request with invalid data type for the 'did' field, expecting a Bad Request response."
      - get:
          url: "/me"
          expect:
            - statusCode: 400
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            did: "{{ value }}" # assuming 'value' from csv is of the wrong data type


