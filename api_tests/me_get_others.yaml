# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_get_others.csv"
    fields:
      - "id"
      - "did"
      - "name"
      - "image"
      - "email"
      - "settings"
      - "createdAt"
      - "updatedAt"
    skipHeader: true

scenarios:
  - name: "Account API test"
    flow:
      - think: 5
      - get:
          url: "/me"
          log: "Requesting with invalid URL"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 5
      - get:
          url: "/me"
          log: "Requesting with invalid Authorization header"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "schema_field"
            - hasProperty: "value"
            - hasProperty: "field"
      - think: 5
      - get:
          url: "/me"
          log: "Requesting with valid token but expecting the API to return a bad request"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "schema_field"
            - hasProperty: "value"
            - hasProperty: "field"
