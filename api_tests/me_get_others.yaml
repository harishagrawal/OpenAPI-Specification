# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}

scenarios:
  - name: "Test scenario for /me endpoint"
    flow:
      - log: "Sending request to /me without authorization"
        get:
          url: "/me"
          expect:
            - statusCode: 401
            - hasProperty: "headers.api-version"
            - hasProperty: "content.application/json.schema"
            - equals:
                - "{{ content.application/json.schema.properties.error.example }}"
                - "error/code"
        think: 5

      - log: "Sending request to /me with invalid authorization"
        get:
          url: "/me"
          headers:
            Authorization: "InvalidAuth"
          expect:
            - statusCode: 401
            - hasProperty: "headers.api-version"
            - hasProperty: "content.application/json.schema"
            - equals:
                - "{{ content.application/json.schema.properties.error.example }}"
                - "error/code"
        think: 5

      - log: "Sending request to invalid /me endpoint"
        get:
          url: "/m"
          expect:
            - statusCode: 400
            - hasProperty: "headers.api-version"
            - hasProperty: "content.application/json.schema"
        think: 5
