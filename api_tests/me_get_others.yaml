# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_get_others.csv"
    fields:
      - "id"
      - "did"
      - "name"
      - "image"
      - "email"
      - "settings"
      - "createdAt"
      - "updatedAt"
    skipHeader: true

scenarios:
  - name: "Testing /me endpoint"
    flow:
      - log: "Testing without authentication"
        get:
          url: "/me"
          headers:
            authorization: "wrong_token"
          expect:
            - statusCode: 401
            - hasProperty: 'headers.api-version'
            - hasProperty: 'content.application/json'
            - equals: 
                - "{{ response.content.application.json.schema.required }}"
                - [ 'description', 'error' ]
        think: 1

      - log: "Testing with invalid URL"
        get:
          url: "/invalid_url"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: 'headers.api-version'
            - hasProperty: 'content.application.json'
            - equals: 
                - "{{ response.content.application.json.schema.required }}"
                - [ 'description', 'error', 'schema_field', 'value' ]
        think: 1

      - log: "Testing with invalid data"
        post:
          url: "/me"
          headers:
            authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            did: "{{ did }}"
            name: "{{ name }}"
            image: "{{ image }}"
            email: "invalid_email"
            settings: "{{ settings }}"
            createdAt: "{{ createdAt }}"
            updatedAt: "{{ updatedAt }}"
          expect:
            - statusCode: 400
            - hasProperty: 'headers.api-version'
            - hasProperty: 'content.application.json'
            - equals: 
                - "{{ response.content.application.json.schema.required }}"
                - [ 'description', 'error', 'schema_field', 'value' ]
        think: 1
