# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "me_get_others.csv"
    fields: ["field", "value"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "API Failure Scenarios"
    flow:
      - log: "Sending request with invalid payload"
        post:
          url: "/me"
          headers:
            Authorization: "InvalidToken"
          json:
            field: "{{ field }}"
            value: "{{ value }}"
        expect:
          - statusCode: 400
          - hasProperty: "error"
          - hasProperty: "description"
          - equals:
              error: "error/code"
              description: "Descriptive Error Message"
        # This request will result in 400 Bad Request, 
        # as the payload is invalid and the token is incorrect.

      - think: 5

      - log: "Sending request without Authorization"
        post:
          url: "/me"
          headers:
            Authorization: ""
          json:
            field: "{{ field }}"
            value: "{{ value }}"
        expect:
          - statusCode: 401
          - hasProperty: "error"
          - hasProperty: "description"
          - equals:
              error: "error/code"
              description: "Descriptive Error Message"
        # This request will result in 401 Authentication Required, 
        # as the Authorization header is missing.

      - think: 5

      - log: "Sending request to invalid URL"
        post:
          url: "/invalid_url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            field: "{{ field }}"
            value: "{{ value }}"
        expect:
          - statusCode: 404
        # This request will result in 404 Not Found, 
        # as the URL is incorrect.
