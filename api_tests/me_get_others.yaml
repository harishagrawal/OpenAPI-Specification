# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/me_get_others.csv"
    fields:
      - "invalidDid"
      - "invalidName"
      - "invalidImage"
      - "invalidEmail"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Negative Test Cases for /me API"
    flow:
      - think: 3
      - comment: "Test with an invalid authorization token to simulate authentication failure"
        post:
          url: "/me"
          headers:
            Authorization: "Bearer invalid_auth_token_should_fail"
          log: "Testing with invalid authorization should result in 401 Unauthorized response"
          expect:
            - statusCode: 401
            - hasHeader: 'api-version'
            - hasProperty: 'error'

      - think: 3
      - comment: "Test with an invalid URL to simulate a bad request"
        get:
          url: "/invalid_me_url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Testing with an invalid URL should result in 400 Bad Request response"
          expect:
            - statusCode: 400
            - hasHeader: 'api-version'
            - hasProperty: 'error'

      - think: 3
      - comment: "Test with an invalid DID in the authentication header to simulate a bad request"
        get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            DID: "{{ invalidDid }}"
          log: "Testing with an invalid DID should result in 400 Bad Request response"
          expect:
            - statusCode: 400
            - hasHeader: 'api-version'
            - hasProperty: 'error'

      - think: 3
      - comment: "Test with an invalid name parameter in the request body to validate the input pattern handling"
        post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            name: "{{ invalidName }}"
          log: "Testing with an invalid name pattern should result in 400 Bad Request response"
          expect:
            - statusCode: 400
            - hasHeader: 'api-version'
            - hasProperty: 'error'
            - hasProperty: 'schema_field'

      - think: 3
      - comment: "Test with an invalid image format in the request body to validate the input pattern handling"
        post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            image: "{{ invalidImage }}"
          log: "Testing with an invalid image should result in 400 Bad Request response"
          expect:
            - statusCode: 400
            - hasHeader: 'api-version'
            - hasProperty: 'error'
            - hasProperty: 'schema_field'

      - think: 3
      - comment: "Test with an invalid email pattern in the request body to ensure proper format validation"
        post:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "{{ invalidEmail }}"
          log: "Testing with an invalid email pattern should result in 400 Bad Request response"
          expect:
            - statusCode: 400
            - hasHeader: 'api-version'
            - hasProperty: 'error'
            - hasProperty: 'schema_field'

