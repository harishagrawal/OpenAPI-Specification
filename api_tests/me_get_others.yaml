# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_get_others.csv"
    fields:
      - "did"
      - "name"
      - "image"
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Negative test scenarios for /me API"
    flow:
      - think: 2
      - comment: "Test case to check the API response when an invalid 'Authorization' header is passed"
        post:
          auth:
            bearer: "InvalidToken123"
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
          log: "Passing an invalid Authorization header should result in a 401 error with an appropriate error message."

      - think: 2
      - comment: "Test case to check the API response when invalid url is passed"
        get:
          url: "/invalidEndpoint" # Invalid URL case
          expect:
            - statusCode: 400
            - hasProperty: "error"
          log: "Using an invalid endpoint should result in a 400 error with an appropriate error message."

      - think: 2
      - comment: "Test case to check the API response when a non-uuid 'id' is passed in the payload"
        get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            id: "not-a-uuid"
            did: "{{ did }}"
            name: "{{ name }}"
            image: "{{ image }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
          log: "Sending a non-uuid 'id' should yield a 400 error with an appropriate error message about the 'id' field being invalid."

      - think: 2
      - comment: "Test case to check the API response when an invalid 'name' pattern is passed in the payload"
        get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            name: "1234567890"
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
          log: "Passing a 'name' that doesn't match the required pattern should result in a 400 error and an appropriate error message."

      - think: 2
      - comment: "Test case to check the API response when an invalid 'email' pattern is passed in the payload"
        get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            email: "not-an-email"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
          log: "Passing an 'email' that doesn't match the email pattern should result in a 400 error and an appropriate error message about the 'email' field being invalid."

