# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ae6b8ce5ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "me_get_others.csv"
    fields:
      - "id"
      - "did"
      - "name"
      - "image"
      - "email"
      - "settings"
      - "createdAt"
      - "updatedAt"
    skipHeader: true

scenarios:
  - name: "Artillery test for /me endpoint"
    flow:
      - log: "Testing with invalid URL"
        get:
          url: "/invalid-url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Testing with invalid authorization"
        get:
          url: "/me"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasProperty: "error"
      - think: 1
      - log: "Testing with invalid header"
        get:
          url: "/me"
          headers:
            InvalidHeader: "InvalidValue"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
      - think: 1
      - log: "Testing with missing header"
        get:
          url: "/me"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasProperty: "error"
      - think: 1
