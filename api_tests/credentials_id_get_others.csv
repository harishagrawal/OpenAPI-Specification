import csv
import random
import string
import faker

# Create a Faker instance
fake = faker.Faker()

# Define the variable names
variables = ["id", "name", "email", "date_of_birth", "phone_number"]

# Open a CSV file in write mode
with open("dummy_data.csv", "w", newline='') as file:
    writer = csv.writer(file)

    # Write the column headers
    writer.writerow(variables)

    # Generate 10 rows of dummy data
    for _ in range(10):
        # Generate a random id (sometimes as a string, sometimes as an integer)
        if random.choice([True, False]):
            id = ''.join(random.choices(string.ascii_letters + string.digits, k=5))
        else:
            id = random.randint(1, 1000)

        # Generate a random name
        name = fake.name()

        # Generate a random email (sometimes as a string, sometimes as an integer)
        if random.choice([True, False]):
            email = fake.email()
        else:
            email = random.randint(1, 1000)

        # Generate a random date of birth
        date_of_birth = fake.date_of_birth(minimum_age=18, maximum_age=90)

        # Generate a random phone number (sometimes as a string, sometimes as an integer)
        if random.choice([True, False]):
            phone_number = fake.phone_number()
        else:
            phone_number = random.randint(1, 1000)

        # Write the row to the CSV file
        writer.writerow([id, name, email, date_of_birth, phone_number])
