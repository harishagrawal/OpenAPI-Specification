import csv
import random
import faker

# Initialize Faker library
fake = faker.Faker()

# Define the column headers
headers = ['id', 'name', 'email', 'date_of_birth', 'phone_number', 'ID']

# Open the CSV file in write mode
with open('dummy_data.csv', 'w', newline='') as csvfile:
    # Create a CSV writer
    writer = csv.writer(csvfile)

    # Write the headers to the CSV file
    writer.writerow(headers)

    # Generate 10 rows of dummy data
    for _ in range(10):
        # Generate a random row of data
        row = [
            random.randint(1, 100),  # id
            fake.name() if random.random() < 0.9 else random.randint(1, 100),  # name
            fake.email() if random.random() < 0.9 else random.randint(1, 100),  # email
            fake.date_of_birth(minimum_age=18, maximum_age=90) if random.random() < 0.9 else '',  # date_of_birth
            fake.phone_number() if random.random() < 0.9 else '',  # phone_number
            str(random.randint(1, 100)) if random.random() < 0.9 else fake.name(),  # ID
        ]

        # Write the row to the CSV file
        writer.writerow(row)
