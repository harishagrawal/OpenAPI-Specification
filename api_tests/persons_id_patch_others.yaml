# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=1416aa2295

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "persons_id_patch_others.csv"
    fields:
      - "id"
      - "initials"
      - "firstName"
      - "middleName"
      - "lastName"
      - "birthDate"
      - "birthPlace"
      - "phone"
      - "email"
      - "postalCode"
      - "houseNumber"
      - "houseNumberPostfix"
      - "countryCode"
      - "reference"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Invalid API request tests"
    flow:
      - log: "Sending request with invalid URL"
        patch:
          url: "/recipients/persons/invalidid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            initials: "{{ initials }}"
            firstName: "{{ firstName }}"
            middleName: "{{ middleName }}"
            lastName: "{{ lastName }}"
            birthDate: "{{ birthDate }}"
            birthPlace: "{{ birthPlace }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
            postalCode: "{{ postalCode }}"
            houseNumber: "{{ houseNumber }}"
            houseNumberPostfix: "{{ houseNumberPostfix }}"
            countryCode: "{{ countryCode }}"
            reference: "{{ reference }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
      - log: "Sending request with invalid Authorization"
        patch:
          url: "/recipients/persons/{{ id }}"
          headers:
            Authorization: "InvalidAuthToken"
          json:
            id: "{{ id }}"
            initials: "{{ initials }}"
            firstName: "{{ firstName }}"
            middleName: "{{ middleName }}"
            lastName: "{{ lastName }}"
            birthDate: "{{ birthDate }}"
            birthPlace: "{{ birthPlace }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
            postalCode: "{{ postalCode }}"
            houseNumber: "{{ houseNumber }}"
            houseNumberPostfix: "{{ houseNumberPostfix }}"
            countryCode: "{{ countryCode }}"
            reference: "{{ reference }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
