# Test generated by RoostGPT for test cred-wallet-integration-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "scenario-dff35151d7.csv"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "scenario: Create and verify account"
    flow:
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            Content-Type: "application/json"
            commit-hash: "{{ commitHash }}"
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - hasProperty: "id"
            - hasProperty: "firstName"
            - hasProperty: "lastName"
            - hasProperty: "email"
          log: "Create new account with specified information"
      - think: 1
      - get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "email"
            - hasProperty: "settings"
            - hasProperty: "createdAt"
            - hasProperty: "updatedAt"
          log: "Verify the account created using GET request for /me"
