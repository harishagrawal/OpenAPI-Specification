# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /logout_post for http method type POST in artillery framework
# RoostTestHash=64d33906e5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  defaults:
    headers:
      # Assume the authorization needed is ApiKeyAuth, token provided in AUTH_TOKEN environment variable
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
  payload:
    path: "logout_post_success.csv"
    fields:
      - "username"
      - "userId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Logout Scenario"
    flow:
      - post:
          url: "/logout"
          # This is expected to successfully log out a user
          log: "Attempting to log out a valid user session"
          json:
            username: "{{ username }}"
            userId: "{{ userId }}"
          headers:
            # Assume the content type is required as 'application/json'
            content-type: "application/json"
          expect:
            - statusCode: 200
            - hasHeader: 'api-version'
          think: 1

      - post:
          url: "/logout"
          # This represents a bad logout attempt due to invalid credentials
          log: "Attempting to log out with invalid credentials"
          json:
            username: "invalid_username"
            userId: "invalid_id"
          headers:
            content-type: "application/json"
          expect:
            - statusCode: 400
            - hasHeader: 'api-version'
            - body:
                match: '$.error'
                equals: 'error/code'
          think: 1

