# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /logout_post for http method type POST in artillery framework
# RoostTestHash=64d33906e5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  processor: "./my_processor.js"
  plugins:
    expect: {}
  payload:
    path: "logout_post_success.csv"
    skipHeader: true
    fields:
      - "username"
      - "password"
scenarios:
  - name: "Logout Test Scenario"
    flow:
      - log: "Logout API Test - Successful Operation"
      - post:
          url: "/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: headers.api-version
            - equals:
                - "headers.api-version"
                - "1.0.0"
          - think: 1
      - log: "Logout API Test - Bad Request"
      - post:
          url: "/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: headers.api-version
            - equals:
                - "headers.api-version"
                - "1.0.0"
            - hasProperty: "error"
            - hasProperty: "description"
          - think: 1
