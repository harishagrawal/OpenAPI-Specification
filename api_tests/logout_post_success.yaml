# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=64d33906e5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    - path: "logout_post_success.csv"
      fields:
        - "apiKey"
        - "apiVersion"
      skipHeader: true
  plugins:
    expect: {}
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - flow:
      # Logout scenario, should succeed with status 200 and appropriate headers
      - post:
          url: "/logout"
          log: "Logging out, expecting successful operation with status 200 and API version header present."
          headers:
            X-Api-Key: "{{ apiKey }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - body: "{\"api-version\":\"{{ apiVersion }}\"}"  # This assert might need modification based on actual response structure
      - think: 1

