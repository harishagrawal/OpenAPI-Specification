# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=64d33906e5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  processor: "./processors.js"
  plugins:
    expect: {}
  payload:
    path: "logout_post_success.csv"
    fields:
      - "error"
      - "description"
      - "schema_field"
      - "value"
      - "field"
    skipHeader: true
  env:
    AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
    
scenarios:
  - name: "Logout Success and Failure Scenarios"
    flow:
      - post:
          url: "/logout"
          headers:
              Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Logout should delete the session and return a 200 status with API version header."
          # Expecting a successful logout with a 200 response
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
          think: 1

      - post:
          url: "/logout"
          headers:
              Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Logout with invalid token should return a 400 status with an error message."
          # Simulating an invalid authorization token leading to a 400 Bad Request response
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
            - hasProperty: "error"
            - hasProperty: "description"
          think: 1

