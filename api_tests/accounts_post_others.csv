import csv
import random
import faker

# Initialize Faker library
fake = faker.Faker()

# Define the column headers
headers = ['email', 'id', 'name', 'date_of_birth', 'phone_number']

# Open the output CSV file
with open('output.csv', 'w', newline='') as csvfile:
    writer = csv.writer(csvfile)

    # Write the headers to the CSV file
    writer.writerow(headers)

    # Generate 100 rows of data
    for _ in range(100):
        # Generate random values for each field
        email = str(random.randint(100, 999)) if random.random() < 0.1 else fake.email()
        id = fake.random_number(digits=5) if random.random() < 0.1 else fake.pystr_format()
        name = fake.name()
        date_of_birth = '' if random.random() < 0.1 else fake.date_of_birth(minimum_age=22, maximum_age=90)
        phone_number = fake.pystr_format() if random.random() < 0.1 else fake.phone_number()

        # Write the row to the CSV file
        writer.writerow([email, id, name, date_of_birth, phone_number])
