# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true
  environments:
    staging:
      target: "https://staging.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Create new account scenario"
    flow:
      - log: "This request should create a new account with a valid email and return a 201 status code."
        post:
          url: "/accounts"
          headers:
            commit-hash: "{{ commitHash }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: "api-version"
            - hasProperty: "commit-hash"
        think: 1

      - log: "This request should correctly handle an invalid email in the payload and return a 400 status code."
        post:
          url: "/accounts"
          headers:
            commit-hash: "{{ commitHash }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "invalid-email@domain"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        think: 1

