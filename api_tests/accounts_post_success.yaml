# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "/mnt/data/accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true

scenarios:
  - name: "Create new Account success scenario"
    flow:
      - post:
          url: "/accounts"
          # Creating an account with a valid email
          # Expecting 201 response with API version and commit-hash headers
          headers: 
            commit-hash: "928d28d"
          json:
            email: "{{ email }}"
          log: "Creating an account with a valid email"
          expect:
            - statusCode: 201
            - hasProperty: ['api-version', 'commit-hash']
          think: 1

