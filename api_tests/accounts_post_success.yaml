# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true
scenarios:
  - name: "Create new Account"
    flow:
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            commit-hash: "928d28d"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 201
              hasProperty: ['headers.api-version', 'headers.commit-hash']
          log: "Post request to create new account"
      - think: 1
