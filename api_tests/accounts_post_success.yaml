# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test scenario for creating new accounts"
    flow:
      - post:
          url: "/accounts"
          headers:
            commit-hash: "928d28d" # Example commit hash from spec
          json:
            email: "{{ email }}"
          log: "Testing API with a valid email to create a new account"
          think: 1
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - hasHeader: "commit-hash"
          # The expected behavior is to create a new account and receive an email with OTP. 
          # It should return 201 with headers indicating the API version and commit hash.

