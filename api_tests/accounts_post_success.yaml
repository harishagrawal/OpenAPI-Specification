# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true
scenarios:
  - name: "Create new Account"
    flow:
      - post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            commit-hash: "928d28d"
          capture:
            - json: "$"
              as: "response"
          expect:
            - statusCode: 201
            - hasProperty: "headers.api-version"
            - hasProperty: "headers.commit-hash"
          beforeRequest:
            think: 1
            log: "Making request to create a new account with email {{ email }}"
      - think: 1
