# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=cb128291e8

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "authorizations_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Test API endpoint /authorizations/{id}"
    flow:
      - log: "Test Case 1 - Invalid request with wrong URL"
        delete:
          url: "/authorizations/wrong-id"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Test Case 2 - Invalid request with no authorization token"
        delete:
          url: "/authorizations/{{ id }}"
          headers:
            Authorization: "Bearer wrong-token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
      - log: "Test Case 3 - Invalid request with malformed id"
        delete:
          url: "/authorizations/123"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
