# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=018c225aea

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test various failure scenarios for GET /credentials/{id}"
    flow:
      - log: "Request with non-UUID id should result in 400 Bad Request"
      - get:
          url: "/credentials/not-a-uuid"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
        think: 1

      - log: "Missing Authorization header should result in 401 Authentication Required"
      - get:
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
        think: 1

      - log: "Invalid Authorization header should result in 401 Authentication Required"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
        think: 1

      - log: "Valid request but non-existing credential ID should result in 400 Bad Request"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
        think: 1

      - log: "Request with malformed URL (validation failure) should result in 400 Bad Request"
      - get:
          url: "/credentials/id=malformed"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasHeader: "api-version"
        think: 1

