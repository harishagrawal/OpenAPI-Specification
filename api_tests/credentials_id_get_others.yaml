# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=018c225aea

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Credential API failure scenarios"
    flow:
      - log: "Sending a request with an invalid id, expecting a 400 error"
        # Expected behavior: The API should return a 400 Bad Request response when the id format is invalid
        get:
          url: "/credentials/invalid-id" 
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"

      - think: 2

      - log: "Sending a request with a missing id, expecting a 400 error"
        # Expected behavior: The API should return a 400 Bad Request response when the id is missing
        get:
          url: "/credentials/" # Invalid URL due to missing id
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"

      - think: 2

      - log: "Sending a request with an invalid authorization token, expecting a 401 error"
        # Expected behavior: The API should return a 401 Unauthorized response when the authorization token is invalid
        get:
          url: "/credentials/{{ id }}" 
          headers:
            Authorization: "InvalidToken" # Invalid authorization token
          expect:
            - statusCode: 401
            - hasProperty: "error"

      - think: 2

      - log: "Sending a request with a valid id but without any auth token, expecting a 401 error"
        # Expected behavior: The API should require an authorization token and return a 401 Unauthorized when it's not provided
        get:
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasHeader: 'api-version'

