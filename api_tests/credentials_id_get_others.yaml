# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=c02d3561ab

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "API endpoint testing"
    flow:
      - log: "Testing with invalid authorization"
        get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Bearer InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "headers.api-version"
            - hasProperty: "content.application/json.schema.properties.error"
            - hasProperty: "content.application/json.schema.properties.description"
      - think: 1

      - log: "Testing with invalid URL"
        get:
          url: "/invalid/credentials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "headers.api-version"
            - hasProperty: "content.application/json.schema.properties.error"
            - hasProperty: "content.application/json.schema.properties.description"
      - think: 1

      - log: "Testing with invalid id parameter"
        get:
          url: "/credentials/invalid-id"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "headers.api-version"
            - hasProperty: "content.application/json.schema.properties.error"
            - hasProperty: "content.application/json.schema.properties.description"
