# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=018c225aea

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "invalidUuid"
      - "otherField"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test credential fetching with various failure cases"
    flow:
      - think: 2

      # Attempt to fetch a credential with an invalid UUID
      - get:
          url: "/credentials/{{ invalidUuid }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
          log: "Fetch credential with an invalid UUID should return Bad Request (400)."
      - think: 2

      # Attempt to fetch a credential with no Authorization header (expect 401 Unauthorized)
      - get:
          url: "/credentials/123e4567-e89b-12d3-a456-426614174000" # sample valid UUID
          expect:
            - statusCode: 401
          log: "Fetch credential without Authorization should return Unauthorized (401)."
      - think: 2

      # Attempt to fetch a credential with invalid Authorization token (expect 401 Unauthorized)
      - get:
          url: "/credentials/123e4567-e89b-12d3-a456-426614174000" # sample valid UUID
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
          log: "Fetch credential with invalid Authorization token should return Unauthorized (401)."
      - think: 2

      # Attempt to fetch a credential with malformed URL
      - get:
          url: "/crentials/123e4567-e89b-12d3-a456-426614174000" # intentionally misspelled URL
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
          log: "Fetch credential with malformed endpoint should return Bad Request (400)."

