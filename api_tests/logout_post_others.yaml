# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=64d33906e5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "logout_post_others.csv"
    fields: ["field1", "field2"]
    skipHeader: true
  processor: "./my_processor.js"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Logout API failure test"
    flow:
      - log: "Failure test - Invalid authorization token"
        post:
          url: "/logout"
          headers:
            ApiKeyAuth: "invalid_auth_token"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
          # Expecting a 400 Bad Request response as the auth token is invalid
      - think: 5
      - log: "Failure test - Invalid URL"
        post:
          url: "/invalid_url"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          # Expecting a 404 Not Found response as the URL is invalid
      - think: 5
      - log: "Failure test - Invalid payload"
        post:
          url: "/logout"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            field1: "invalid_value"
            field2: "invalid_value"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "value"
            - hasProperty: "field"
            - hasProperty: "schema_field"
          # Expecting a 400 Bad Request response as the payload is invalid
