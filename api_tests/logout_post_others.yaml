# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=64d33906e5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "logout_post_others.csv"
    fields:
      - "description"
      - "error"
      - "schema_field"
      - "value"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Logout API failure cases"
    flow:
      - log: "Invalid URL Test"
        post:
          url: "/logut" # Invalid URL
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "api-version"
            - body:
                description: "Not Found"
                error: "error/not-found"
      - think: 1

      - log: "Unauthorized Test"
        post:
          url: "/logout"
          headers:
            Authorization: "invalid_token" # Invalid Token
          expect:
            - statusCode: 401
            - hasHeader: "api-version"
            - body:
                description: "Unauthorized"
                error: "error/unauthorized"
      - think: 1

      - log: "Bad Request Test"
        post:
          url: "/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            description: "{{ description }}"
            error: "{{ error }}"
            schema_field: "{{ schema_field }}"
            value: "{{ value }}"
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
            - body:
                description: "Descriptive Error Message"
                error: "validation/email-pattern"
      - think: 1
