# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=64d33906e5

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "logout_post_others.csv"
    fields:
      - "description"
      - "error"
      - "schema_field"
      - "value"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Logout API failure cases"
    flow:
      - log: "Invalid URL, expecting 400 status code"
        post:
          url: "/logut" # Invalid URL
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        think: 1

      - log: "Invalid Authorization, expecting 400 status code"
        post:
          url: "/logout"
          headers:
            Authorization: "Bearer invalid_token" # Invalid Authorization
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        think: 1

      - log: "Invalid Payload, expecting 400 status code"
        post:
          url: "/logout"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            description: "{{ description }}" # Invalid Payload from CSV
            error: "{{ error }}" # Invalid Payload from CSV
            schema_field: "{{ schema_field }}" # Invalid Payload from CSV
            value: "{{ value }}" # Invalid Payload from CSV
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        think: 1
