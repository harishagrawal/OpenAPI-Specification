# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_post_others.csv"
    fields:
      - "email"
    skipHeader: true
scenarios:
  - name: "API endpoint testing"
    flow:
      - log: "Testing with invalid URL"
        get:
          url: "/invalidurl"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
        expect:
          - statusCode: 404
        think: 1
      - log: "Testing without required email field in payload"
        post:
          url: "/accounts"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          json:
            email: ""
        expect:
          - statusCode: 400
          - hasProperty: "description"
          - hasProperty: "error"
        think: 1
      - log: "Testing with invalid email pattern"
        post:
          url: "/accounts"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          json:
            email: "invalidemail"
        expect:
          - statusCode: 400
          - hasProperty: "description"
          - hasProperty: "error"
        think: 1
      - log: "Testing with wrong authorization token"
        post:
          url: "/accounts"
          headers:
            Authorization: "Bearer wrong_token"
            commit-hash: "928d28d"
          json:
            email: "{{ email }}"
        expect:
          - statusCode: 401
        think: 1
