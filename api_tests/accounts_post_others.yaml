# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_post_others.csv"
    fields:
      - "email"
    skipHeader: true
  processor: "./my_processor.js"

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - log: "Sending a request with missing email in payload"
        post:
          url: "/accounts"
          json:
            email: ""
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        # This request should return a 400 status code with error details in the response body.

      - think: 1

      - log: "Sending a request with invalid email format in payload"
        post:
          url: "/accounts"
          json:
            email: "invalid_email_format"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
            - hasProperty: "schema_field"
            - hasProperty: "value"
        # This request should return a 400 status code with error details in the response body.

      - think: 1

      - log: "Sending a request with invalid authorization token"
        post:
          url: "/accounts"
          json:
            email: "{{ email }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
        # This request should return a 401 status code indicating unauthorized access.

      - think: 1

      - log: "Sending a request to an invalid URL"
        post:
          url: "/invalid_url"
          json:
            email: "{{ email }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        # This request should return a 404 status code indicating the URL could not be found.
