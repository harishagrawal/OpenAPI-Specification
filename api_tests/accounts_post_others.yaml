# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "accounts_post_others.csv"
    fields:
      - "email"
    skipHeader: true

scenarios:
  - name: "Test /accounts endpoint"
    flow:
      - log: "Sending request with missing header"
        post:
          url: "/accounts"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
        think: 1
      - log: "Sending request with invalid email format"
        post:
          url: "/accounts"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          json:
            email: "invalid_email"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "schema_field"
        think: 1
      - log: "Sending request with invalid URL"
        post:
          url: "/invalid_url"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Sending request with invalid Authorization token"
        post:
          url: "/accounts"
          headers:
            Authorization: "Bearer invalid_token"
            commit-hash: "928d28d"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
        think: 1
