# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/accounts_post_others.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Invalid and edge case scenarios for /accounts API"
    flow:
      # Invalid email format in the request body
      - log: "Sending a request with an invalid email format"
      - post:
          url: "/accounts"
          headers:
              authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "invalidemail"
          expect:
            - statusCode: 400
            - hasProperty: error
            - hasProperty: description
      - think: 2
      
      # Fail due to missing required email in the request body
      - log: "Sending a request without an email which is a required field"
      - post:
          url: "/accounts"
          headers:
              authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: error
            - hasProperty: description
      - think: 2

      # Invalid authorization token in the header
      - log: "Sending a request with an invalid authorization token"
      - post:
          url: "/accounts"
          headers:
              authorization: "invalidToken"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401 # Assuming 401 is the proper response for invalid tokens
            - hasProperty: error
            - hasProperty: description
      - think: 2

      # Commit hash with invalid pattern in the header
      - log: "Sending a request with an invalid commit-hash pattern"
      - post:
          url: "/accounts"
          headers:
              authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
              commit-hash: "123"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: error
            - hasProperty: description
      - think: 2

      # Invalid URL test
      - log: "Sending a request to an invalid URL"
      - post:
          url: "/invalid_accounts"
          headers:
              authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
            - hasProperty: error
            - hasProperty: description
      - think: 2
      
      # Sending request with excess headers
      - log: "Sending a request with non-required excess headers"
      - post:
          url: "/accounts"
          headers:
              authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
              extra-header: "non-required"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: error
            - hasProperty: description

