# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "accounts_post_others.csv"
    fields:
      - "email"
    skipHeader: true
scenarios:
  - name: "API endpoint testing for failure cases and edge cases"
    flow:
      - post:
          url: "/accounts"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasProperty: ['api-version', 'commit-hash']
          beforeRequest: "log"
          log: "Posting with valid email"
      - think: 1
      - post:
          url: "/accounts"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          json:
            email: "invalid email"
          expect:
            - statusCode: 400
            - hasProperty: ['api-version']
          beforeRequest: "log"
          log: "Posting with invalid email"
      - think: 1
      - post:
          url: "/invalid-url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            commit-hash: "928d28d"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
          beforeRequest: "log"
          log: "Posting to invalid URL"
      - think: 1
      - post:
          url: "/accounts"
          headers:
            Authorization: "Invalid auth token"
            commit-hash: "928d28d"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
          beforeRequest: "log"
          log: "Posting with invalid authorization token"
