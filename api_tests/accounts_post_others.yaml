# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/accounts_post_others.csv"
    fields:
      - "email"
      - "commit-hash-invalid"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Invalid email format and commit hash header scenario"
    flow:
      - log: "Sending invalid email format to get 400"
      - post:
          url: "/accounts"
          headers:
             Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
             commit-hash: "{{ email }}" # Using email for commit hash to make it invalid
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasProperty: 'description'
      - think: 5
      - log: "Sending invalid authentication token to get 401"
      - post:
          url: "/accounts"
          headers:
             Authorization: "invalid {{ $processEnvironment.AUTH_TOKEN }}"
             commit-hash: "{{ commit-hash-invalid }}"
          json:
            email: "validemail@example.com" # Not using the email from CSV file here
          expect:
            - statusCode: 401
      - think: 5
      - log: "Sending request with omitted required email to get 400"
      - post:
          url: "/accounts"
          headers:
             Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
             commit-hash: "{{ commit-hash-invalid }}"
          json: {}
          expect:
            - statusCode: 400
            - hasProperty: 'error'
            - hasProperty: 'description'
      - think: 5
      - log: "Sending request to invalid URL to produce a 404 error"
      - post:
          url: "/invalid_url"
          headers:
             Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "validemail@example.com"
          expect:
            - statusCode: 404

