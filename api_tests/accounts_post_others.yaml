# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=53e96f9805

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/accounts_post_others.csv"
    fields:
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Create new Account - Negative Tests"
    flow:
      - think: 3
      - post:
          url: "/accounts"
          headers:
            commit-hash: "a1b2c3d" # Valid pattern for commit-hash header
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          log: "Testing invalid email format, expecting 400 Bad Request"
          # The email value comes from the csv and should have an invalid email format
          # Expected: 400 Bad Request for invalid email format
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"

      - think: 3
      - post:
          url: "/accounts"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "test@example.com" # Valid email format
          log: "Testing without commit-hash header, expecting 201 Created as header is optional"
          # Expected: 201 Created since commit-hash is not required
          expect:
            - statusCode: 201
            - hasHeader: "api-version"

      - think: 3
      - post:
          url: "/accoun"
          headers:
            commit-hash: "a1b2c3d" # Valid pattern even for invalid URL test
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "test@example.com" # Valid email format
          log: "Testing with invalid URL, expecting a status code other than 200-299 range"
          # Expected: Status code outside the 200-299 range due to wrong URL
          expect:
            - statusCode: /^(?!2\d\d$)\d{3}$/

      - think: 3
      - post:
          url: "/accounts"
          headers:
            commit-hash: "123" # Invalid pattern for commit-hash header
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "test@example.com" # Valid email format
          log: "Testing with invalid pattern for commit-hash header, expecting 400 Bad Request"
          # Expected: 400 Bad Request due to invalid commit-hash format
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"

      - think: 3
      - post:
          url: "/accounts"
          headers:
            commit-hash: "a1b2c3d" # Valid pattern for commit-hash header
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "test@example.com" # Valid email format
          log: "Testing with invalid Authorization token, expecting status code indicating auth failure"
          # Expected: Status code indicating authorization failure such as 401 Unauthorized
          expect:
            - statusCode: /^(?!2\d\d$)\d{3}$/
            - hasProperty: "description"
            - hasProperty: "error"

