import csv
import random
import faker
from datetime import datetime, timedelta

# create a faker object
fake = faker.Faker()

# define the number of rows and starting id
size = 100
offset = 1

# define the column names
fieldnames = ['id', 'first_name', 'last_name', 'email', 'phone_number', 'date_of_birth', 'address']

# open the csv file in write mode
with open('dummy_data.csv', 'w', newline='') as csvfile:
    writer = csv.DictWriter(csvfile, fieldnames=fieldnames)

    # write the column headers
    writer.writeheader()

    # generate the data rows
    for i in range(offset, offset + size):
        writer.writerow({
            'id': i,
            'first_name': fake.first_name(),
            'last_name': fake.last_name(),
            'email': fake.email(),
            'phone_number': fake.phone_number().replace(",", ""),
            'date_of_birth': (datetime.now() - timedelta(days=random.randint(18*365, 80*365))).strftime('%Y-%m-%d'),
            'address': fake.address().replace(",", "")
        })
