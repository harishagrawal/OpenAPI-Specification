# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=63322ffb26

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "branches_put_others.csv"
    fields: 
      - "cocNumber"
      - "cocBranchNumber"
      - "city"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Recipient Branch API tests"
    flow:
      - log: "Test Case: Invalid URL"
        post:
          url: "/recipients/branchesss"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Test Case: Invalid Authorization Token"
        post:
          url: "/recipients/branches"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Test Case: Missing Required Fields in Request Body"
        post:
          url: "/recipients/branches"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Test Case: Invalid Email in Request Body"
        post:
          url: "/recipients/branches"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "invalidEmail"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
