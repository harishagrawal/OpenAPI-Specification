# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=63322ffb26

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "branches_put_others.csv"
    fields: 
      - "cocNumber"
      - "cocBranchNumber"
      - "city"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Recipient Branch API tests"
    flow:
      - log: "Test Case: Invalid URL"
        put:
          url: "/recipients/branchesss"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Test Case: Invalid cocNumber"
        put:
          url: "/recipients/branches"
          json:
            cocNumber: "invalid"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "value"
            - hasProperty: "schema_field"
      - think: 1
      - log: "Test Case: Invalid cocBranchNumber"
        put:
          url: "/recipients/branches"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "invalid"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "value"
            - hasProperty: "schema_field"
      - think: 1
      - log: "Test Case: Invalid Authorization"
        put:
          url: "/recipients/branches"
          json:
            cocNumber: "{{ cocNumber }}"
            cocBranchNumber: "{{ cocBranchNumber }}"
            city: "{{ city }}"
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          headers:
            Authorization: "Invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
