import csv
import random
import faker

# Initialize Faker library
fake = faker.Faker()

# Define the column headers
headers = ['id', 'name', 'email', 'date_of_birth', 'phone_number', 'ID']

# Open the CSV file in write mode
with open('dummy_data.csv', 'w', newline='') as csvfile:
    writer = csv.DictWriter(csvfile, fieldnames=headers)

    # Write the headers
    writer.writeheader()

    # Generate 100 rows of dummy data
    for i in range(100):
        # Create a dictionary for the row
        row = {}

        # Generate a unique ID
        row['id'] = i + 1

        # Generate a random name or a number
        if random.choice([True, False]):
            row['name'] = fake.name()
        else:
            row['name'] = random.randint(1000, 9999)

        # Generate a random email or a number
        if random.choice([True, False]):
            row['email'] = fake.email()
        else:
            row['email'] = random.randint(1000, 9999)

        # Generate a random date of birth
        row['date_of_birth'] = fake.date_of_birth(minimum_age=20, maximum_age=90)

        # Generate a random phone number or leave it blank
        if random.choice([True, False]):
            row['phone_number'] = fake.phone_number()
        else:
            row['phone_number'] = ''

        # Generate a random ID or a string
        if random.choice([True, False]):
            row['ID'] = fake.random_int(min=1000, max=9999)
        else:
            row['ID'] = fake.random_letters(length=10)

        # Write the row to the CSV file
        writer.writerow(row)
