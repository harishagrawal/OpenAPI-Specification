# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=061355b093

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "branches_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
scenarios:
  - name: "scenario"
    flow:
      - log: "Sending request with valid ID but without authorization"
        get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "Bearer invalid_token"
        expect:
          - statusCode: 401
          - hasProperty: "headers.api-version"
          - hasProperty: "error"
          - hasProperty: "description"
        think: 1
      - log: "Sending request with invalid ID"
        get:
          url: "/recipients/branches/invalid_id"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
          - hasProperty: "headers.api-version"
          - hasProperty: "error"
          - hasProperty: "description"
        think: 1
      - log: "Sending request to invalid URL"
        get:
          url: "/recipients/branches_invalid/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 1
