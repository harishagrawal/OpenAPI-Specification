# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=061355b093

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "branches_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
scenarios:
  - name: "scenario"
    flow:
      - log: "Sending request with invalid URL"
        get:
          url: "/recipients/branches/invalid-url"
          expect:
            - statusCode: 400
            - hasProperty: 'description'
            - hasProperty: 'error'
      - think: 1
      - log: "Sending request without token"
        get:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 401
            - hasProperty: 'description'
            - hasProperty: 'error'
      - think: 1
      - log: "Sending request with invalid token"
        get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: 'description'
            - hasProperty: 'error'
      - think: 1
      - log: "Sending request with valid token but without id"
        get:
          url: "/recipients/branches/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: 'description'
            - hasProperty: 'error'
