# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=061355b093

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "branches_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
  http:
    pool: 10
scenarios:
  - name: "Fail test cases for /recipients/branches/{id}"
    flow:
      - think: 5
      - log: "Test with invalid UUID"
        get:
          url: "/recipients/branches/invalid_uuid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 5
      - log: "Test with no Authorization"
        get:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 5
      - log: "Test with invalid Authorization"
        get:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 5
      - log: "Test with invalid URL"
        get:
          url: "/invalid_url/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
