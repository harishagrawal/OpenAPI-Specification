import csv
import random
import faker

# Initialize Faker library
fake = faker.Faker()

# Define the column headers
headers = ['id', 'name', 'email', 'date_of_birth', 'phone_number', 'address']

# Open a CSV file in write mode
with open('dummy_data.csv', 'w', newline='') as csvfile:
    # Initialize a CSV writer
    writer = csv.writer(csvfile)

    # Write the headers to the CSV file
    writer.writerow(headers)

    # Generate 10 rows of dummy data
    for _ in range(10):
        # Generate a random row of data
        row = [
            random.randint(1, 1000),  # id
            fake.name() if random.random() > 0.2 else '',  # name
            fake.email() if random.random() > 0.2 else random.randint(1, 1000),  # email
            fake.date_of_birth(minimum_age=22, maximum_age=90) if random.random() > 0.2 else '',  # date_of_birth
            fake.phone_number() if random.random() > 0.2 else '',  # phone_number
            fake.address().replace(',', '') if random.random() > 0.2 else '',  # address
        ]

        # Write the row to the CSV file
        writer.writerow(row)
