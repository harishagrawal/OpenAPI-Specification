# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=cb128291e8

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  processor: "./processor.js"
  payload:
    path: "authorizations_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Remove Authorized Account"
    flow:
      - log: "Delete Request for Removing Authorized Account"
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "api-version"
          capture:
            - json: "$.api-version"
              as: "api-version"
      - think: 5
