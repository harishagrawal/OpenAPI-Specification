# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=4affc26394

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "apikeys_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "API Keys Test"
    flow:
      - log: "Starting test on API Keys"
      - get:
          url: "/apikeys/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "api-version"
            - hasProperty: "id"
      - think: 1
      - log: "Successfully retrieved API Key"
      - delete:
          url: "/apikeys/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "api-version"
      - think: 1
      - log: "Successfully deleted API Key"
