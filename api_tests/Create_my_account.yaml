# Test generated by RoostGPT for test cred-wallet-integration-artillery using AI Type Open AI and AI Model gpt-4-1106-preview


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    - path: "Create_my_account.csv"
      skipHeader: true
      fields:
        - "accounts_body"
  plugins:
    expect: {}

scenarios:
  - name: "scenario Create my account"
    flow:
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
          json:
            {{ accounts_body }}
          log: "Creating a new account with provided payload."
          expect:
            - statusCode: 201
            - hasProperty: "id"
          think: 1
      - get:
          url: "/me"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          log: "Verifying the account created by fetching account details."
          expect:
            - statusCode: 200
            - hasProperty: "username"
            - hasProperty: "email"
          think: 1
