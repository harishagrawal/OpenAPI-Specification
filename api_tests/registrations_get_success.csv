import csv
import random
import faker

# Initialize Faker library
fake = faker.Faker()

# Define the headers
headers = ['id', 'credential-url', 'size', 'offset']

# Open or create the CSV file
with open('dummy_data.csv', 'w', newline='') as csvfile:
    writer = csv.DictWriter(csvfile, fieldnames=headers)

    # Write the headers
    writer.writeheader()

    # Generate and write the dummy data
    for _ in range(100):  # Generate 100 rows of dummy data
        writer.writerow({
            'id': random.randint(1000, 9999),  # Generate a random integer as ID
            'credential-url': fake.uri(),  # Generate a fake URL as credential URL
            'size': random.randint(1, 100),  # Generate a random integer as size
            'offset': random.randint(0, 50)  # Generate a random integer as offset
        })
