# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ed8c1b7bff

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_post_others.csv"
    fields:
      - "email"
    skipHeader: true

scenarios:
  - name: "Test failure cases for /authorizations API"
    flow:
      - think: 5

      - log: "Test Case 1: Testing for validation failure with invalid email pattern"
      - post:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            email: "invalid email"
          expect:
            - statusCode: 400
            - hasProperty: "headers.api-version"
            - hasProperty: "error"
            - equals:
                - "{{ response.body.error }}"
                - "validation/email-pattern"
      - think: 5

      - log: "Test Case 2: Testing for authentication failure with invalid auth token"
      - post:
          url: "/authorizations"
          headers:
            Authorization: "invalid token"
            content-type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
            - hasProperty: "headers.api-version"
            - hasProperty: "error"
      - think: 5

      - log: "Test Case 3: Testing for failure when invalid URL is passed"
      - post:
          url: "/authorizations_invalid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 404
