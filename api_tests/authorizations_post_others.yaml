# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ed8c1b7bff

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_post_others.csv"
    fields: ["email"]
    skipHeader: true
  http:
    pool: 10
scenarios:
  - name: "Authorizations API endpoint test"
    flow:
      - log: "Starting the test scenario for /authorizations API"
      - think: 1
      - post:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
          beforeRequest:
            log: "Sending a POST request to /authorizations with invalid email"
      - think: 1
      - post:
          url: "/authorizations"
          headers:
            Authorization: "InvalidToken"
            content-type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasProperty: "error"
          beforeRequest:
            log: "Sending a POST request to /authorizations with invalid authorization token"
      - think: 1
      - post:
          url: "/invalid_authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 400
          beforeRequest:
            log: "Sending a POST request to invalid URL"
      - think: 1
      - log: "Finished the test scenario for /authorizations API"
