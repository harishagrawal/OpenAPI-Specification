# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /apikeys_post for http method type POST in artillery framework
# RoostTestHash=28a54f1579

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "apikeys_post_success.csv"
    fields: ["id", "token", "createdAt"]
    skipHeader: true

scenarios:
  - name: "API keys - Create API Key"
    flow:
      - log: "Creating API Key"
      - post:
          url: "/apikeys"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "{{ id }}"
            token: "{{ token }}"
            createdAt: "{{ createdAt }}"
          expect:
            - statusCode: 201
            - hasProperty: "id"
            - hasProperty: "token"
            - hasProperty: "createdAt"
            - hasHeader: "api-version"
            - hasHeader: "location"
      - think: 1
