# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=9caaa2ef96

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "branches_id_patch_others.csv"
    fields:
      - "id"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
scenarios:
  - name: "Test the /recipients/branches/{id} API endpoint"
    flow:
      - log: "First test request: Testing invalid authorization"
        patch:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasProperty: "error"
        think: 1
      - log: "Second test request: Testing invalid payload"
        patch:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            tradeName: 1234567890
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        think: 1
      - log: "Third test request: Testing invalid URL"
        patch:
          url: "/recipients/branches/invalid-id"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        think: 1
