# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=9caaa2ef96

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "branches_id_patch_others.csv"
    fields:
      - "id"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
scenarios:
  - name: "Test the /recipients/branches/{id} API endpoint"
    flow:
      - log: "First test request: Testing invalid URL"
        patch:
          url: "/invalidURL"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Second test request: Testing invalid authorization"
        patch:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "Bearer invalid_token"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "description"
      - think: 1
      - log: "Third test request: Testing invalid request body"
        patch:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            tradeName: null
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "schema_field"
            - hasProperty: "value"
      - think: 1
      - log: "Fourth test request: Testing invalid email format in request body"
        patch:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "invalid_email_format"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "description"
            - hasProperty: "schema_field"
            - hasProperty: "value"
