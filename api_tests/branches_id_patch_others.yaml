# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=9caaa2ef96

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 1
  plugins:
    expect: {}
  payload:
    path: "branches_id_patch_others.csv"
    fields:
      - "id"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true

scenarios:
  - name: "Test the Recipient Branch API"
    flow:
      - log: "Test with invalid uuid in the path"
        patch:
          url: "/recipients/branches/invalid_uuid"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
        think: 5

      - log: "Test with invalid authorization token"
        patch:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "Invalid token"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "{{ email }}"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasProperty: "error"
        think: 5

      - log: "Test with invalid email format in the request body"
        patch:
          url: "/recipients/branches/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            tradeName: "{{ tradeName }}"
            phone: "{{ phone }}"
            email: "invalid email"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
            - hasProperty: "schema_field"
            - hasProperty: "value"
        think: 5
