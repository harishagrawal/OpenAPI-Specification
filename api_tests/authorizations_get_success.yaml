# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4


# Test generated for /authorizations_get for http method type GET in artillery framework
# RoostTestHash=3b64f22044

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "./authorizations_get_success.csv"
    fields:
      ["id", "name", "email", "createdAt", "updatedAt"]
    skipHeader: true

scenarios:
  - name: "Authorizations API testing"
    flow:
      - log: "Starting Authorizations API testing"
      - get:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "authorizations"
            - hasHeader: "api-version"
          capture:
            json: "$.authorizations"
            as: "authorizations"
      - think: 1
      - log: "Completed Authorizations API testing"
