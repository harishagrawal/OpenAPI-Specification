# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=3b64f22044

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/authorizations_get_success.csv"
    fields:
      - "id"
      - "name"
      - "email"
      - "createdAt"
      - "updatedAt"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "List accounts who have access to my account"
    flow:
      - get:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          # The request fetches the accounts that have access to the user's account
          log: "Fetching the list of authorized accounts"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "total"
            - hasProperty: "authorizations"
          think: 1

