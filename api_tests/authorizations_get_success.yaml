# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# Test generated for /authorizations_get for http method type GET in artillery framework
# RoostTestHash=3b64f22044

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "./authorizations_get_success.csv"
    fields:
      - "id"
      - "name"
      - "email"
      - "createdAt"
      - "updatedAt"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Test /authorizations API"
    flow:
      - log: "Starting test on /authorizations API"
      - get:
          url: "/authorizations"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasProperty: "total"
            - hasProperty: "authorizations"
            - hasHeader: "api-version"
          capture:
            - json: "$.total"
              as: "total"
            - json: "$.authorizations"
              as: "authorizations"
      - think: 1
      - log: "Tested for successful operation on /authorizations API"
