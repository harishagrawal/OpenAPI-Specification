import csv
import random
import faker

# Initialize Faker Library
fake = faker.Faker()

def generate_dummy_data(num_records):
    # Define column names
    fieldnames = ['id', 'first_name', 'last_name', 'email', 'address', 'phone_number', 'date_of_birth']

    # Create a new CSV file
    with open('dummy_data.csv', 'w', newline='') as csvfile:
        writer = csv.DictWriter(csvfile, fieldnames=fieldnames)

        # Write column headers
        writer.writeheader()

        # Generate and write dummy data
        for i in range(num_records):
            writer.writerow({
                'id': i+1,
                'first_name': fake.first_name(),
                'last_name': fake.last_name(),
                'email': fake.email(),
                'address': fake.address().replace(',', ' '),  # Replace commas to maintain CSV format
                'phone_number': fake.phone_number(),
                'date_of_birth': fake.date_of_birth(minimum_age=22, maximum_age=90).isoformat(),
            })

# Generate 100 records
generate_dummy_data(100)
