import csv
import random
import faker

# Initialize faker generator
fake = faker.Faker()

# Define CSV file name
csv_file = "dummy_data.csv"

# Define CSV headers
headers = ['id', 'name', 'email', 'date_of_birth', 'phone_number']

# Open CSV file
with open(csv_file, 'w', newline='') as file:
    writer = csv.writer(file)

    # Write headers
    writer.writerow(headers)

    # Generate dummy data
    for i in range(100):  # Generate 100 rows of data
        row = [
            i+1,  # id
            fake.name(),  # name
            fake.email(),  # email
            fake.date_of_birth(minimum_age=22, maximum_age=60).strftime('%Y-%m-%d'),  # date_of_birth
            fake.phone_number()  # phone_number
        ]

        # Write row to CSV
        writer.writerow(row)
