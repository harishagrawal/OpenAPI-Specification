# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=aa0a5d4980

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Test Recipient Person API"
    flow:
      - log: "Testing with invalid URL"
        get:
          url: "/invalid/persons"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Testing without authorization"
        get:
          url: "/recipients/persons"
          headers:
            Authorization: "Invalid"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing with invalid size parameter"
        get:
          url: "/recipients/persons?size=60"
          expect:
            - statusCode: 400
            - hasProperty: ['description', 'error']
      - think: 1
      - log: "Testing with invalid offset parameter"
        get:
          url: "/recipients/persons?offset=-1"
          expect:
            - statusCode: 400
            - hasProperty: ['description', 'error']
      - think: 1
      - log: "Testing with invalid name parameter"
        get:
          url: "/recipients/persons?name=123"
          expect:
            - statusCode: 400
            - hasProperty: ['description', 'error']
      - think: 1
      - log: "Testing with invalid birth-date parameter"
        get:
          url: "/recipients/persons?birth-date=invalid"
          expect:
            - statusCode: 400
            - hasProperty: ['description', 'error']
