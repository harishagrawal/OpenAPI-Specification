# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4

# RoostTestHash=aa0a5d4980

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "./persons_get_others.csv"
    fields:
      - "name"
      - "birthDate"
    skipHeader: true
  environments:
    development:
      target: "{{ $processEnvironment.API_HOST }}"
      variables:
        AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Recipient Persons API failure test cases"
    flow:
      - log: "Testing invalid name query parameter"
        get:
          url: "/recipients/persons?name=123"
          headers:
            Authorization: "{{ AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
      - think: 1
      - log: "Testing invalid birthDate query parameter"
        get:
          url: "/recipients/persons?birthDate=abcd"
          headers:
            Authorization: "{{ AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
      - think: 1
      - log: "Testing without required authorization header"
        get:
          url: "/recipients/persons"
          expect:
            - statusCode: 401
            - hasProperty: "description"
            - hasProperty: "error"
      - think: 1
      - log: "Testing invalid URL"
        get:
          url: "/recipients/persons_invalid"
          headers:
            Authorization: "{{ AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
      - log: "Testing invalid size query parameter"
        get:
          url: "/recipients/persons?size=abc"
          headers:
            Authorization: "{{ AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
      - think: 1
      - log: "Testing invalid offset query parameter"
        get:
          url: "/recipients/persons?offset=abc"
          headers:
            Authorization: "{{ AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "description"
            - hasProperty: "error"
