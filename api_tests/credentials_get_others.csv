import random
import csv
import faker
from datetime import datetime, timedelta

# initialize faker module
fake = faker.Faker()

def generate_dummy_values(size: int, offset: int):
    # Column headers
    headers = ['ID', 'Name', 'Email', 'Phone Number', 'Date of Birth', 'Gender', 'Country']

    # Generate CSV file
    with open('dummy_data.csv', 'w', newline='') as csvfile:
        writer = csv.writer(csvfile)

        # Write headers
        writer.writerow(headers)

        for _ in range(size):
            # Generate random ID (sometimes as string, sometimes as number)
            if random.choice([True, False]):
                id = fake.random_number(digits=5)
            else:
                id = fake.word()

            # Generate random name
            name = fake.name()

            # Generate random email (sometimes as string, sometimes as number)
            if random.choice([True, False]):
                email = fake.email()
            else:
                email = fake.random_number(digits=5)

            # Generate random phone number
            phone = fake.phone_number()

            # Generate random date of birth
            dob = fake.date_of_birth(minimum_age=20, maximum_age=70)

            # Generate random gender
            gender = fake.random_element(elements=('Male', 'Female', ''))

            # Generate random country
            country = fake.country()

            # Write data to CSV
            writer.writerow([id, name, email, phone, dob, gender, country])

    print(f"Generated 'dummy_data.csv' with {size} rows of data.")

# Call the function
generate_dummy_values(1000, 10)
