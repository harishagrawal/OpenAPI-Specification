import csv
import random
import faker
from datetime import datetime, timedelta

# initialize faker module
fake = faker.Faker()

def generate_dummy_values(size: int, offset: int):
    # open a csv file in write mode
    with open('dummy_values.csv', 'w', newline='') as file:
        writer = csv.writer(file)
        # write the headers
        writer.writerow(["ID", "Name", "Email", "DOB", "Phone Number"])
        
        for i in range(offset, offset + size):
            # generate random values
            id = str(i) if random.choice([True, False]) else i
            name = fake.name()
            email = fake.email() if random.choice([True, False]) else str(i)
            dob = fake.date_of_birth(minimum_age=20, maximum_age=50).isoformat() if random.choice([True, False]) else str(i)
            phone_number = fake.phone_number() if random.choice([True, False]) else str(i)
            
            # write the values to the csv file
            writer.writerow([id, name, email, dob, phone_number])

generate_dummy_values(size=10, offset=0)
