# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview


# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=f1fc1d06f7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "/mnt/data/credentials_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: Delete a credential from the Wallet
    flow:
      - comment: "Expecting successful deletion of credential with valid UUID"
        get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: 'api-version'
          log: "Successfully deleted the credential with provided UUID."
        think: 1

