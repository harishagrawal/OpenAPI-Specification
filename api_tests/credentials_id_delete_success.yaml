# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=f1fc1d06f7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Delete credential from Wallet"
    flow:
      - comment: "Send a DELETE request to delete a credential by ID with valid UUID and expect a 200 response with correct api-version header"
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
        log: "Deleting a credential with a valid UUID should return 200 and the api-version header"
      - think: 1
      
      - comment: "Send a DELETE request without authorization token to expect a 401 response with correct content"
      - delete:
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 401
            - hasHeader: "api-version"
        log: "Attempting deletion without an authorization token should result in a 401 with the api-version header"
      - think: 1

      - comment: "Send a DELETE request with an incorrect UUID format to expect a 400 response with correct content"
      - delete:
          url: "/credentials/incorrect-uuid-format"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "api-version"
        log: "Deleting a credential with an invalid UUID format should return 400 and the api-version header"
      - think: 1

