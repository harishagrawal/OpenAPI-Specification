# Test generated by RoostGPT for test credential-wallet-api using AI Type Open AI and AI Model gpt-4-1106-preview

# RoostTestHash=f1fc1d06f7

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Delete credential operation"
    flow:
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          # Expect successful deletion of a credential with a given ID
          log: "Attempting to delete the credential with ID {{ id }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
              headerValue: "1.0.0"
          think: 2

      # Additional delete tests with fixed valid IDs can be included here if necessary
      # - delete:
      #     ...
      #   think: 2

